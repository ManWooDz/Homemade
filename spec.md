# Homemade — สร้างสูตรอาหารไทยจากวัตถุดิบในตู้เย็น ที่ grounded และตรวจสอบได้

Capstone โปรเจกต์ ทีม 2 คน กำลังเตรียมสอบหัวข้อ (ปีการศึกษา 2569) รายละเอียดเชิงวิชาการเต็มดูที่ [docs/project-proposal.md](docs/project-proposal.md)

## Architecture

**Stack ปัจจุบัน (โค้ดจริงใน repo):**
- Backend: FastAPI (Python) + SQLite (`backend/main.py`) — SQL แบบ raw ผ่าน `sqlite3.connect()` ไม่มี ORM
- Frontend: React + Vite + Tailwind CSS v4 (`frontend/src/pages/*`)
- AI: Gemini API (`gemini-3.1-flash-lite-preview`) เรียกตรงใน `call_agentic_llm()`
- มี 6-stage validation pipeline อยู่แล้ว (`validate_recipe()`): ingredient check, flavor check, logic check, nutrition check (bound เดียว 0-1500 cal ไม่มี DB อ้างอิง), allergy check (keyword matching ผ่าน `ALLERGEN_MAP`), core-ingredient check
- มีไฟล์ `backend/model/yolo11n_food.pt` อยู่ในโปรเจกต์แต่**ไม่ได้ใช้งานแล้ว** (ทีมตัดสินใจไม่เอา YOLO แล้ว — อย่าเสนอกลับมา)

**Stack เป้าหมาย (กำลังจะสร้าง):**
- Backend DB: ย้าย SQLite → **PostgreSQL** (พร้อม SQLAlchemy แนะนำ เพราะตอนนี้ raw SQL string จะรกเมื่อมี user_id ผูกหลายตาราง)
- Auth: JWT ทำเอง (`bcrypt` ตรง — **ไม่ใช่** `passlib[bcrypt]`, verify แล้วว่าคู่กับ bcrypt>=4.0 พัง ดู Current State step 8 — + `python-jose` + FastAPI `OAuth2PasswordBearer`/`OAuth2PasswordRequestForm`) — **ไม่ใช่** fastapi-users, **ไม่ใช่** Auth.js/BetterAuth (เป็น Node-only ใช้กับ FastAPI ไม่ได้) — **เสร็จแล้ว** (`backend/auth.py`, `/api/auth/register`, `/api/auth/login`, `/api/auth/me`)
- ตั้ง `hashed_password` เป็น nullable ไว้เผื่ออนาคตอยากเพิ่ม Google OAuth (เพิ่มได้แบบ additive ไม่ต้องรื้อ ไม่ต้องทำตอนนี้)
- External API: Open Food Facts (barcode lookup, fallback พิมพ์เองถ้าไม่เจอ — coverage ของสดตลาดไทยอ่อน)

**System Architecture diagram:** ทำใน Eraser.io (นอก repo) — ดูส่วน Current State ด้านล่างสำหรับจุดที่ต้องแก้

## Done

- Frontend หน้าใช้งานหลักครบ: AddIngredient, CreateRecipe, UserIngredients, RecipeDetail, CustomCookingPage, CookingPage (มี flow ทั้งเลือก base menu และ generate อิสระ)
- Backend 6-stage validation pipeline (ดูด้านบน) — เป็นฐานที่ core contribution ใหม่จะต่อยอด ไม่ใช่สร้างใหม่หมด
- `docs/project-proposal.md` — เอกสารข้อเสนอโครงงานฉบับเต็ม (ที่มาปัญหา, objectives, scope, architecture, features, feasibility, success criteria, Gantt ~12 เดือน [ต้องบีบเหลือ ~9 เดือนจริง — ยังไม่ได้แก้], การแบ่งงาน 2 คน)
- Competitive analysis: Paprika, Cooklang, Deglaze, Umami, Spillt, ReciMe, SuperCook, AI Chatbot ทั่วไป — สรุปว่า gap ของตลาดคือ "generative + บริบทไทย + grounded/ตรวจสอบได้พร้อมกัน" ไม่มีเจ้าไหนทำครบ

## Todo / Out of scope

**Contribution หลัก (แคบ ลึก ต้องมี evaluation — อย่าขยายเพิ่มอีก):**
1. Nutrition Engine: Thai quantity parsing → unit normalization → lookup ฐานข้อมูล INMU (สถาบันโภชนาการ ม.มหิดล — **ยังไม่ยืนยันว่าเข้าถึงข้อมูลได้จริง**, fallback USDA FoodData Central) → (ถ้าเวลาพอ) retention/yield factor ตามวิธีปรุง
2. Allergen Validation 2-layer: rule-based keyword (ต่อยอด `ALLERGEN_MAP` เดิม) → Knowledge Graph (transitive, เช่น กะปิ→กุ้ง) → LLM hidden-allergen layer — ลำดับนี้อยู่ *ภายใน* step 5 ของ 6-step check เดิม ไม่ใช่ stage แยกต่างหาก
   - **ตัดสินใจแล้ว (2026-07): ทำเป็น Knowledge Graph จริง** ไม่ใช่ flat JSONB tag — ต้องมี node/edge/traversal จริงถึงจะ claim คำนี้ได้อย่างซื่อสัตย์ (ไม่งั้นเข้า trap เดียวกับที่เคยพลาดเรื่อง "RAG" มาก่อน)
   - Schema: table `allergen_edges` (ingredient_id, implies_allergen_or_ingredient_id) + recursive CTE query — **อยู่ใน PostgreSQL ตัวเดิม ไม่ต้องสลับไป Neo4j/graph DB เฉพาะทาง** (ปรึกษาอาจารย์มาว่าต้องเปลี่ยน DB แต่จริงๆ ไม่จำเป็น — graph = data structure concept ไม่ผูกกับ storage engine, ใช้ edge-list + recursive query บน relational DB เป็น pattern มาตรฐาน)
   - **ข้อบังคับสำคัญ:** dataset ที่ curate ต้องมีเคส **2-hop ขึ้นไปจริง** (เช่น "น้ำพริกกะปิ/ลูกชิ้นกะปิ" → กะปิ → shrimp) ไม่ใช่แค่ 1-hop ทั้งหมด ถ้าทุก edge เป็น 1-hop เดียว ต่อให้ implement เป็น edge table ก็ยังเทียบเท่า flat array ในทางปฏิบัติ (ไม่มี inference จริงให้โชว์) — ต้องออกแบบ dataset ให้มี chain จริงเพื่อพิสูจน์ claim ตอนเดโม/สอบ
3. Agentic Critic-Feedback Loop: แก้ blind-retry ปัจจุบันให้ส่ง fail reason กลับเข้า prompt รอบถัดไป (โค้ดร่างอยู่ด้านล่าง Current State)

**Product envelope (ความเสี่ยงวิจัยต่ำ CRUD/API ปกติ — โชว์เดโมได้ ไม่ผูก KPI วิชาการ):**
- Barcode Scanner (Open Food Facts + fallback)
- Auth: Login/Profile, JWT
- Favorite, Rating (ดาว 1-5), Food Generate History ต่อ user
- Personalization: inject history/rating เป็น context ใน prompt ผ่าน **plain query ธรรมดา** (top-rated/recent history, ไม่ใช่ RAG) (**demonstrated only** — ไม่วัด "ดีขึ้นเมื่อใช้นาน" เพราะ cold-start)
  - ~~ตัดสินใจ 2026-07: ไม่ทำ RAG~~ **พลิกกลับ (2026-07): ทำ RAG จริง** ด้วย embedding + pgvector — ยังคง**ไม่นับเป็น complex component ตัวที่ 4** ของ contribution หลัก (nutrition/allergen-graph/critic-loop ยังคงเป็น 3 ตัวหลักที่มี evaluation) RAG นี้ยังอยู่ใต้ personalization ที่ **demonstrated only ไม่ผูก KPI** เหมือนเดิม แค่ implementation จริงเปลี่ยนจาก plain query เป็น embedding-based retrieval
  - **Embedding model ตัดสินใจแล้ว: Gemini Embedding** (`gemini-embedding-001`/Gemini Embedding 2 Preview) — **ไม่ใช้ Qwen** (text-embedding-v4) เหตุผล: (1) Gemini ยืนยันรองรับไทยชัดเจนใน official doc, Qwen แค่ "100+ languages" กว้างๆ ไม่ยืนยันไทย (2) free tier Gemini เป็น rate-limit ต่อเนื่องไม่มีวันหมดอายุ, Qwen free 1M token **หมดอายุใน 90 วัน** เสี่ยงหมดกลางโปรเจกต์ (3) provider เดียวกับ Gemini generation เดิม ไม่ต้องเปิด account/API key/SDK ใหม่ — ตัด tongyi-embedding-vision-plus/flash ทิ้ง (เป็น vision embedding ไม่เกี่ยวกับ text-only task นี้), qwen3-rerank ก็ไม่ใช้ (ผูกกับ Qwen ecosystem)
  - **ต้องทำก่อนใช้จริง:** ทดสอบ embed คู่ข้อความไทยที่ความหมายใกล้/ไกลกัน เช็คว่า similarity score แยกออกจริงไหม ก่อนเชื่อ (ยังไม่เคย verify Thai quality ของ Gemini Embedding โดยตรง แค่ confirm ว่า "รองรับ" ใน doc)
  - **Schema เพิ่ม:** `generate_history` เพิ่ม column `embedding VECTOR(768)` (ต้องเปิด `pgvector` extension ใน PostgreSQL), query ด้วย cosine similarity (`<=>` operator) เทียบ history ที่ rating สูง
  - **Verified (2026-09-07):** `gemini-embedding-001` default คืน **3072 มิติ** ไม่ใช่ 768 — ต้องส่ง `config=types.EmbedContentConfig(output_dimensionality=768)` ทุกครั้งตอนเรียก `embed_content()` ถึงจะได้ 768 จริง (ทดสอบแล้วว่า MRL truncation ใช้งานได้ผ่าน param นี้) ห้ามลืม param นี้ตอนเขียนโค้ดจริง ไม่งั้น insert ลง `VECTOR(768)` column แล้ว error dimension mismatch ทันที
  - **Legacy plain-query design (เก็บไว้อ้างอิง ไม่ใช่แผนที่ใช้แล้ว):**
    1. Tag-rating aggregate: `SELECT tag, COUNT(*), AVG(rating) FROM generate_history, unnest(diet_tags) AS tag WHERE user_id=X AND rating IS NOT NULL GROUP BY tag ORDER BY avg_rating DESC` → หา flavor/menu-type ที่ user ให้ rating สูง
    2. Ingredient×dish-category cross-tab: `SELECT ingredient, tag, COUNT(*) FROM generate_history, unnest(adjusted_ingredients) AS ingredient, unnest(diet_tags) AS tag WHERE user_id=X GROUP BY ingredient, tag HAVING COUNT(*) >= 3` → หา pattern "user มักใส่วัตถุดิบ X ในเมนูประเภท Y" — ใส่ `HAVING COUNT(*) >= 3` กัน noise จาก data น้อย (capstone timeframe มี record จำกัด ยิ่ง cross-tab ละเอียดยิ่งต้องการ data เยอะกว่าจะมีนัยสำคัญ)
    3. แปลงผล query เป็น text summary สั้นๆ ("มักชอบเมนูรสจัด ผัด/แกง, มักใส่ไข่ในเมนูผัด") → ฉีดเข้า prompt ตำแหน่งเดียวกับ "Preferences (+History)"
  - **ตัดออกจาก scope:** สัญญาณ "วัตถุดิบที่มักมีใน fridge เสมอ" — ต้องมี audit log การ add/remove วัตถุดิบข้ามเวลาซึ่งยังไม่ได้วางแผน (query fridge ปัจจุบันบอก pattern อดีตไม่ได้) เพิ่มทีหลังได้ถ้ามีเวลา ไม่ใช่ MVP
  - **คำที่ต้องใช้ตอนเขียนเล่ม/สอบ:** เรียก "rating-weighted aggregation heuristic" หรือ "rule-based taste profile summarizer" — **ห้ามเรียกว่า "AI เรียนรู้ preference"** เพราะไม่มี model train จริง เป็นแค่ SQL aggregation
- Expiry Tracking + ranking (**demonstrated only** — ไม่เคลม "ลด food waste X%")
- PostgreSQL migration + schema ใหม่ (users, ingredient_nutrition, generate_history, favorites, ratings, user_ingredients+expiry_date) — แผนเตรียมการละเอียดดูที่ [docs/postgres-migration-prep.md](docs/postgres-migration-prep.md)
  - **2026-09-07 progress:** step 1 (verify embedding dimension) เสร็จแล้ว — ดูรายละเอียดที่ RAG section ด้านบน. step 2 (install packages) เสร็จแล้ว — เพิ่ม `sqlalchemy>=2.0`, `psycopg[binary]`, `alembic`, `pgvector`, `passlib[bcrypt]`, `python-jose[cryptography]` เข้า `backend/requirements.txt` และ install จริงใน `backend/venv` แล้ว (verify ผ่าน `import` สำเร็จ) — **ยังไม่ได้แก้โค้ด main.py/fridge_repository.py**. step 3 (docker-compose local Postgres) — สร้างไฟล์ `docker-compose.yml` แล้ว (`pgvector/pgvector:pg16`, db=`homemade`, port 5432) **แต่ verify รันจริงไม่ได้** เพราะเครื่อง dev นี้ไม่มี `docker` ติดตั้ง — ผู้ใช้ต้องรัน `docker compose up -d` เองแล้วเช็ค `docker compose ps` ว่า healthy
  - **step 4 เสร็จ (2026-09-07):** `backend/database/models.py` — SQLAlchemy 2.0 model 9 ตาราง (`users`, `user_ingredients`+`expiry_date`, `base_recipes`, `ingredient_nutrition`, `ingredient_nodes`, `allergen_edges`, `generate_history`, `favorites`, `ratings`) verify แล้วว่า import + build metadata ผ่าน (ยังไม่ต่อ DB จริงเพราะ docker ไม่ขึ้น)
  - **Schema decision เพิ่มจาก step 4 (ไม่ได้อยู่ใน spec เดิม):**
    - เพิ่มตาราง `ingredient_nodes` (id, name unique, node_type CHECK 'ingredient'/'allergen') แยกจาก `ingredient_nutrition` — `allergen_edges` FK เข้า `ingredient_nodes` ไม่ใช่ `ingredient_nutrition` เพราะ allergen category (เช่น "shellfish") กับเมนูผสม (เช่น "น้ำพริกกะปิ") ไม่มี macro nutrition ถ้าฝืน FK เข้า nutrition table จะมี row NULL ปนใน Nutrition Engine ที่ต้องวัดผลจริง
    - `allergen_edges` มี `UNIQUE(ingredient_id, implies_id)` + `CHECK (ingredient_id <> implies_id)` กัน self-loop/duplicate ที่จะทำ recursive CTE ค้างหรือ garbage ตอนเดโม
    - `favorites`/`ratings` ชี้เข้า `generate_history.id` เท่านั้น (ไม่ทำ polymorphic FK ไป base_recipes ด้วย) — เลือกทางแก้คือ log การเลือก base-menu เข้า `generate_history` ด้วย (คอลัมน์ `source` CHECK 'base'/'generated' + `base_recipe_id` nullable FK)
    - `generate_history.adjusted_ingredients` และ `diet_tags` ใช้ Postgres `ARRAY(String)` จริง ไม่ใช่ JSONB — เพราะ spec.md query เดิม (บรรทัด personalization) ใช้ `unnest()` ซึ่งต้องเป็น array type ไม่ใช่ JSON
    - ยังไม่ใส่ ivfflat/HNSW index บน `embedding` column ในรอบนี้ — index พวกนี้ต้องมีข้อมูลอยู่แล้วถึงจะ build ได้ดี ตารางยังว่างเปล่า เพิ่มทีหลังตอนมีข้อมูลจริง
  - **step 5 เสร็จ (2026-09-08):** ผู้ใช้ติดตั้ง Docker Desktop + รัน `docker compose up -d` สำเร็จ, verify `psql` เข้าได้จริง (PostgreSQL 16.15). ทำ `alembic init alembic` ใน `backend/`, แก้ `alembic/env.py` ให้อ่าน `DATABASE_URL` จาก `.env` (เพิ่ม `DATABASE_URL=postgresql+psycopg://homemade:homemade_dev_only@localhost:5432/homemade` ใน `backend/.env`) และผูก `target_metadata = Base.metadata` จาก `database/models.py`
    - **Bug เจอตอน autogenerate แก้แล้วก่อน apply:** (1) migration ไม่มี `CREATE EXTENSION vector` มาให้เอง — เพิ่ม `op.execute('CREATE EXTENSION IF NOT EXISTS vector')` เป็นบรรทัดแรกของ `upgrade()` เอง (2) migration ใช้ `pgvector.sqlalchemy.vector.VECTOR(dim=768)` แต่ไม่มี `import pgvector.sqlalchemy` มาให้ — รันจริงจะ `NameError`, เพิ่ม import เอง
    - `alembic upgrade head` รันผ่าน, verify ด้วย query `information_schema.tables` จริง — ครบ 9 ตาราง (+ `alembic_version`) และ `pg_extension` มี `vector` ติดตั้งแล้ว
    - ไฟล์ที่เพิ่ม: `backend/alembic/` (ทั้ง dir), `backend/alembic.ini`, `backend/database/models.py`, `docker-compose.yml` (root); แก้ `backend/.env` (เพิ่ม `DATABASE_URL`), `backend/requirements.txt`
  - **step 6 เสร็จ (2026-09-08):** `main.py`/`fridge_repository.py` ตัด `sqlite3` ออกทั้งหมด ต่อ Postgres จริงผ่าน SQLAlchemy (`database/db.py` — `get_db()` FastAPI dependency)
    - `backend/database/models.py` แก้ 2 จุดจาก step 4: `user_ingredients.quantity` ต้อง **nullable** (พลาดไว้ตอน step 4 — เป็น product decision เดิมว่า insert ใหม่ต้องเก็บ `quantity=NULL` เสมอ "presence not quantity" tracking, มี unit test pin ไว้); เพิ่มตาราง `recipe_ingredient_images` (name unique, image) แยกจาก `ingredient_nutrition` — เหตุผลเดียวกับ `ingredient_nodes`: เป็น presentation data (path รูปสำหรับ map ingredient→รูปตอนแสดงสูตร, มาจาก `setup_ingredients.py` เดิม) ไม่ใช่ nutrition data ห้ามปนกัน
    - Migration ใหม่แยก revision (`5bae32b93b0a`, ไม่แก้ revision แรกที่ apply ไปแล้ว) — verify `alembic downgrade base && upgrade head` round-trip สะอาด
    - **Dev-user bridge จนกว่าจะมี JWT (step 8):** `main.py` มี `DEV_USER_EMAIL="dev@local"` + `get_dev_user_id(db)` resolve ทุก request (ไม่ใช้ module-level global กันค่า stale) — ทุกจุดที่ใช้ยังไม่ผูก auth จริง จุดพวกนี้ต้องตามหา (`grep get_dev_user_id`) ตอนทำ step 8 แล้วเปลี่ยนเป็น authenticated user
    - **Seed data (ดึง step 7 มาทำล่วงหน้าบางส่วน เพราะ verify step 6 ไม่ได้ถ้าตารางว่าง):** `backend/database/seed_postgres.py` พอร์ต 9 mock recipes + ingredient image mapping จาก `setup_db.py`/`setup_ingredients.py` เดิม (ตอนนี้ deprecated แล้ว มี comment กำกับ ไม่ได้ลบไฟล์ตาม Hard Rule) รันแล้วจริง: seed สำเร็จ 9 base_recipes, 55 recipe_ingredient_images, 1 dev user
    - **Verify จริงด้วย HTTP, ไม่ใช่แค่ compile:** รัน `uvicorn main:app` ต่อ Postgres จริง — `GET /api/recipes` คืน 9 เมนูพร้อม mapped ingredient image ถูกต้อง, `GET/POST/DELETE /api/user-ingredients` ทำงานครบ (POST แล้ว GET เห็นจริง, DELETE ซ้ำคืน "not found" ถูกต้อง เพราะรอบใหม่ scope query ด้วย `user_id`), `python -m unittest discover -s tests` ผ่านทั้ง 26 test (รวม `test_fridge_repository.py` ที่ port เป็น SQLAlchemy Session บน in-memory SQLite แล้ว)
  - **ยังไม่ได้ทำตอนนั้น:** `favorites`/`ratings`/`generate_history`/allergen graph ยังไม่มี endpoint ต่อเข้า, ingredient_nutrition ยังไม่มีข้อมูล (รอ Nutrition Engine ตัว contribution หลัก — คนละงานกับ DB migration)
  - **step 8 เสร็จ (2026-09-08): JWT auth จริง** — `backend/auth.py` ใหม่ (`hash_password`/`verify_password`/`create_access_token`/`get_current_user`), endpoint ใหม่ `POST /api/auth/register`, `POST /api/auth/login` (OAuth2PasswordRequestForm), `GET /api/auth/me`
    - **Deviate จาก spec.md:16 (`passlib[bcrypt]`) — ใช้ `bcrypt` ตรงแทน:** verify แล้วว่า `passlib` 1.7.4 + `bcrypt` 5.0.0 (คู่ที่ pip resolve ให้) พังจริง (`AttributeError: module 'bcrypt' has no attribute '__about__'` ตามด้วย `ValueError` ตอน hash) เพราะ passlib อ่าน `bcrypt.__about__.__version__` ที่ bcrypt 4+ เอาออกไปแล้ว — เปลี่ยนไปเรียก `bcrypt.hashpw`/`bcrypt.checkpw` ตรง ไม่ผ่าน passlib, ตัด `passlib[bcrypt]` ออกจาก `requirements.txt` เหลือ `bcrypt` เฉยๆ (ของเดิมติดมาแล้วตอนติดตั้ง `passlib[bcrypt]` ไม่ต้อง install ใหม่)
    - `JWT_SECRET_KEY` generate ด้วย `secrets.token_hex(32)` เก็บใน `backend/.env` (verify แล้วว่า `.env` อยู่ใน `.gitignore` จริง ไม่ถูก track — เช็คก่อนเพราะไฟล์นี้มี API key จริงหลายตัวอยู่แล้ว)
    - **Scope ตัดสินใจ:** gate เฉพาะ endpoint auth ใหม่ (`/api/auth/*`) เท่านั้น — **ไม่แตะ** `/api/recipes`, `/api/user-ingredients` (ยังใช้ dev-user bridge เดิม) เพราะ frontend (`AuthContext.jsx`) ยัง mock อยู่ ไม่เคยส่ง `Authorization` header จริง ถ้า gate endpoint เดิมตอนนี้จะ 401 ทุกหน้าจอ fridge ทันที — เป็น regression ที่ไม่มีใครขอ รอ frontend ต่อ auth จริงก่อนค่อยย้าย `get_dev_user_id` → `get_current_user` (grep หาได้จากคอมเมนต์ `DEV_USER_EMAIL` ใน main.py)
    - **ไม่ได้ทำ:** `favorites`/`ratings`/`generate_history` endpoint ยังไม่สร้าง (คนละ feature จาก "ต่อ auth ให้ใช้งานได้" ตัดสินใจแยกสโคปไว้ ไปทำเป็นงานถัดไป)
    - Verify: unit test ใหม่ 3 ตัว (`test_auth.py` — bcrypt round-trip, reject `hashed_password=NULL`, JWT round-trip) รวม 29/29 ผ่าน; HTTP จริงผ่าน uvicorn+Postgres: register→login→`/me` ด้วย token จริงผ่าน, login รหัสผิด 401, `/me` token ปลอม 401, `/me` ไม่มี token 401, register email ซ้ำ 400, `/api/user-ingredients` เดิมยังทำงานปกติไม่ถูก gate

**Out of scope (ตัดสินใจแล้ว อย่าเสนอกลับมา):**
- YOLO ingredient recognition (ตัดออกแล้ว ใช้ Barcode แทน)
- Meal planning เต็มรูป/shopping list
- ฟีเจอร์โซเชียล (public feed, follow, comment — บทเรียนจาก Spillt)
- Mobile native app, OAuth ภายนอกจริงจัง (Google/Facebook login — เพิ่มทีหลังได้แบบ additive), production-scale deployment
- ทำอาหารจริงทุกสูตรเพื่อทดสอบ (ใช้ 3-layer verification: rule-based safety + retrieval similarity + expert-rated sample เล็กแทน)

## Current state

**Local LLM Generator + Gemini Critic — Milestone 1 implemented AND benchmarked for real (2026-09-14 code, 2026-09-18 real benchmark run, merged to `main` at `acd61c81`) — not a 4th contribution:**
- Advisor asked to evaluate a local trained model replacing Gemini, or local-primary + Gemini-verifies. Brainstormed with user + external review (Codex). Design: [`docs/superpowers/specs/2026-09-13-local-llm-generator-design.md`](docs/superpowers/specs/2026-09-13-local-llm-generator-design.md); Plan: [`docs/superpowers/plans/2026-09-14-local-llm-generator-milestone1.md`](docs/superpowers/plans/2026-09-14-local-llm-generator-milestone1.md); ledger: `.superpowers/sdd/2026-09-14-local-llm-generator-milestone1/progress.md`
- **Maps into existing contribution slots, no 4th contribution opened:** local model (Qwen Instruct, candidate sizes 4B / 7B–9B, no fine-tuning yet) becomes primary generator; Gemini retained as critic — specifically the already-planned "LLM hidden-allergen layer" (layer 3 of Allergen Validation contribution, **not built this milestone**, deferred). **Correction from initial framing (still holds):** the Critic-Feedback Loop's blind-retry→feedback fix is **not** open work — it already shipped 2026-07 (commit `0949f304`). `MEMORY.md`'s "Blind-retry ไม่ใช่ critic-feedback loop" entry has been updated to note the fix and flag this exact gotcha (a real, billed Gemini API call almost recurred twice more during this milestone's TDD — see below).
- **What was built (additive only, zero existing files modified except `README.md`):** `backend/generators/{base,prompts,gemini_generator,local_generator}.py` (pluggable `RecipeGenerator` interface, shared prompt builder with a few-shot path for the local model, `GeminiGenerator` as a new parallel twin of `call_agentic_llm()` — **`main.py`/`call_agentic_llm()` itself is completely untouched, the live endpoint's behavior has not changed at all**), `backend/eval/{metrics.py,run_benchmark.py,fixtures/generator_eval_cases.json}` (benchmark harness reusing the real `validate_recipe()` pipeline read-only, comparing Qwen 4B / Qwen 7B-9B / Gemini on schema validity, explicit-allergen avoidance, ingredient hallucination, and latency — **not yet run for real numbers, no vLLM server exists yet**), plus 5 new test files (27 new tests). Full backend suite: **148/148 passing.**
- **Real, recurring risk class found and fixed 3 times in this milestone:** any test/script that falls back to `os.getenv("GEMINI_API_KEY")` when a key isn't explicitly passed will make a REAL, BILLED call to production Gemini in a dev environment whose `.env` has a real key — not a hypothetical, it actually happened once during Task 3's TDD. Fixed via `@patch.dict(os.environ, ...)` isolation (Tasks 3, 4) and a corrected `run_benchmark.py` docstring + amended plan-doc smoke-test instructions (final review, since the plan's own Task 6 Step 4 as originally written would have triggered it a 3rd time). Logged as a standing gotcha in `MEMORY.md` for any future generator/API-calling test in this codebase.
- **Final whole-branch review (opus) caught 3 Important findings, 1 fix wave, all verified addressed:** (1) an eval fixture (`shrimp_allergy_should_avoid_shrimp_and_shrimp_paste`) set `"allergy": "กุ้ง"` with no trigger keyword, so `main.py`'s `check_allergy()` silently skipped it entirely — the fixture could never fail the check it was named for, regardless of what a generator produced. **This is the exact "Circularity trap"/"Terminology overclaiming" failure class already logged in `MEMORY.md`** — a metric that claims to measure something it structurally cannot. Fixed: `"allergy": "แพ้กุ้ง"` (verified via direct trace of `main.py`'s real keyword list and allergen map that the check now actually fires). (2) The Gemini-vs-Qwen benchmark comparison silently varies both model AND prompt (Gemini uses `include_example=False`, Qwen uses `include_example=True` — intentional per design, but undocumented) — now an explicit code comment; the 4B-vs-7B comparison is the clean, prompt-matched one. (3) `run_benchmark.py`'s docstring inaccurately implied running with no credentials was always safe — corrected.
- **Rulings made (parked, not fixed, with reasoning):** `check_ingredient_hallucination`'s substring-match has real false negatives on Thai homophone-adjacent names (e.g. "ไก่" ⊂ "ไข่ไก่") — plan-mandated design, accepted as a known heuristic limitation for this milestone (proper Thai tokenization is out of scope), now explicitly documented in the function's own docstring so it's never silently over-trusted. `summarize_run` computes mean latency only, though the plan's prose once said "mean/median" — the plan's own code block only ever specified mean; kept as shipped, revisit if the eval set grows past hand-built n=3.
- **1.5-month milestone scope (โครงงานพิเศษ 1) — what this delivers:** generator abstraction interface (`generate(..., feedback=None)`, swappable Gemini↔local via config) + `LocalLLMGenerator` via vLLM + prompt/schema engineering + a benchmark harness. **Still deferred, unchanged:** rewiring the live `/api/generate-recipe-text` endpoint (separate, future, separately-confirmed `/backend/main.py`-touching task); Gemini critic layer wiring; `LocalLLMGenerator` feedback-injection parity with `GeminiGenerator`; Evaluations 2 (critic effectiveness) and 3 (blind-retry vs feedback-loop, empirically).
- **Terminology guardrail (unchanged from design):** base-menu lookup via existing `base_recipes` table is "Base Recipe Retrieval / Template Grounding", **not RAG** — reserved for the separate Personalization RAG (pgvector). Gemini-checking-its-own-Gemini-output would be "second-pass semantic critique"; only Qwen→Gemini would be "cross-model verification using a different model family/provider" — neither claim applies yet since the critic layer isn't built.
- **Merged:** `worktree-local-llm-generator-milestone1` branch merged to `main` (`acd61c81`), branch+worktree deleted. No `/backend/` file was modified without the explicit checkpoint confirmation the user gave before Task 1 (14 new files, listed and confirmed).

**Real benchmark run (2026-09-18) — RunPod Secure Cloud, real GPU, real money spent (~$0.86 total across the whole session, well under the $10 top-up):**
- **Model-size correction (NO MAGIC catch before spending real money):** the design/plan's placeholder "Qwen 4B / 7B–9B" doesn't exist — Qwen2.5 has no 4B or 9B (only 0.5B/1.5B/3B/7B/14B/32B/72B); verified via web search before provisioning anything. Used **`Qwen/Qwen3.5-4B`** and **`Qwen/Qwen3.5-9B`** instead (real, confirmed on Hugging Face + official vLLM recipes) — closer to the original intent than the Qwen3 fallback (4B/8B) considered first. Fixed the hardcoded model strings in `backend/eval/run_benchmark.py`'s `candidates` dict to match (small, confirmed backend edit).
- **GPU/infra lessons (each cost real trial-and-error before landing on a working setup):**
  1. RunPod GPU stock and driver version vary **per physical host**, not per GPU model name — two separate L4 pods landed on old-driver (CUDA 12.8) hosts before `minCudaVersion: "13.0"` on `create-pod` forced a CUDA-13-capable host. vLLM's own compiled extensions (`vllm._C_stable_libtorch`) require `libcudart.so.13` regardless of which torch build is paired with it — pinning torch to cu128 via `uv --torch-backend=cu128` did **not** fix this; only landing on an actual CUDA-13-driver host did.
  2. `runpodctl pod create` (no template volume) defaults to a 20GB container disk with no separate persistent volume — filled up mid-install. Needed explicit `--container-disk-in-gb 60`. Pods created via the MCP `create-pod` tool with a `templateId` get a 50GB mount automatically; plain `runpodctl` CLI creation does not — the two paths aren't equivalent.
  3. `Qwen/Qwen3.5-9B` in bf16 alone uses ~18.2GB VRAM, leaving too little headroom for KV cache on a 24GB card even at a reduced `--max-model-len` — needed `--quantization fp8` to actually fit and serve. The "fits comfortably on a 24GB GPU" claim from vLLM's own recipe docs implicitly assumed quantization, not stated explicitly.
  4. One GPU type (RTX PRO 6000 Blackwell MIG 1g.24gb slice) failed entirely with `RuntimeError: FlashInfer requires GPUs with sm75 or higher` — a MIG-slice/attention-backend incompatibility, not a driver/CUDA version issue. Switched to a plain (non-MIG) A100 80GB instead of debugging further, given time cost.
  5. `ssh <host> '<cmd1>; sleep N; <cmd2>'` (a `sleep` in the same SSH invocation as other commands) can drop the channel (exit 255) per `runpod-usage`'s own documented gotcha — confirmed empirically once. Split into separate SSH calls instead.
- **Model behavior lesson (would have silently broken every local-model benchmark row):** `Qwen/Qwen3.5-*` are hybrid thinking/non-thinking models — by default they emit reasoning into a separate `reasoning` field and leave `message.content` **null** until reasoning finishes, which often doesn't happen within any reasonable token budget (500 tokens of reasoning on a two-line Thai greeting, still unfinished). Fixed by adding `"chat_template_kwargs": {"enable_thinking": false}` to `LocalLLMGenerator`'s request body (`backend/generators/local_generator.py`, confirmed backend edit) — without this, `content` is null and every case scores as an unparseable failure, not a real quality signal.
- **Results — 4B tested 3 times (2 different GPUs), 9B tested once, Gemini baseline re-run each time (real billed calls, small):**

  | Candidate | Schema validity | Hallucination rate | Mean latency |
  |---|---|---|---|
  | Gemini (baseline) | 0.33–0.67 (varies run to run, no fixed seed) | 0.0 | ~4–8.7s |
  | Qwen3.5-4B | 0.0–0.33 (consistent across 3 runs) | 0.67–1.0 (consistent) | ~1.6–14.7s (hardware-dependent) |
  | Qwen3.5-9B (fp8) | 0.67 (1 run only) | 0.33 | ~18.9s |

  4B's poor result is **repeated evidence, not a single-run fluke** — 3 independent runs on 2 different GPU types all showed low validity and a **repeatable qualitative failure**: non-Thai tokens (Greek, Chinese script) leaking into generated Thai recipe text. 9B has only one real run so far and needs a repeat before being trusted the same way.
- **Decision: not fine-tuning yet.** Two of the plan's own preconditions for fine-tuning aren't met: (1) benchmark evidence is still thin for 9B (n=3, one run) and 4B's one-run-worse-than-Gemini gap doesn't yet prove prompting can't close it; (2) no real failure cases from actual usage exist — only the 3 hand-built synthetic fixtures. Recommended next steps instead: try fixing 4B via prompting (explicit "Thai only" instruction, stronger few-shot) before writing it off; get a second 9B run; then wire the Gemini critic layer + `LocalLLMGenerator` feedback parity + Evaluations 2/3 (all still deferred from Milestone 1) before revisiting training with real usage-derived failure cases.
- **Prompt-fix attempt (2026-09-18, 4th real 4B run, A100 SXM):** added a local-model-only rule to `build_recipe_prompt()` (`backend/generators/prompts.py`, gated behind `include_example=True` so it never touches `GeminiGenerator`'s production-matched prompt): fields that must be Thai may still use English where the schema already calls for it (recipe_name/diet_tags), but must never contain a third script (Greek/Chinese/Korean/Cyrillic) with no reason to be there. Result: **schema_validity_rate and hallucination_rate came back identical to the prior run (0.33 / 0.67)** — the garbled tokens that did appear looked milder (`"ข่า 1 โ SDL"` vs. earlier full non-Latin words), but n=3 is too small to call that a real improvement over noise. One case also newly errored after a 48s hang rather than returning a parseable failure — a new failure mode, not previously seen. **Conclusion: this specific prompt tweak did not clearly fix 4B.** Leaning further toward "4B not viable for this task without deeper work (better few-shot, or accept 9B as the local candidate)" — still not a final call given the small sample.
- **9B retest (2026-09-18, 2nd real 9B run, fresh L4 pod, same fp8/3072-context config):** schema_validity_rate came back **identical** to the first run (0.667/0.667 — a genuinely consistent signal across 2 runs). But hallucination_rate swung from 0.33 to **1.0**, and one case showed a new, more severe failure: the model dumped raw English/Hindi-script reasoning text directly into the `unknown_ingredients` field (`"मे triggered by rule or context? No, vegetables not listed..."`) — **despite `chat_template_kwargs.enable_thinking: false` being set**, meaning that flag does not fully suppress reasoning leakage into `content` for this model, at least not reliably under fp8 quantization. **Revised conclusion: 9B is not clearly better than 4B after all** — schema validity looks consistent and reasonably good, but 9B has its own real, repeatable-looking failure mode (reasoning-text leakage) distinct from 4B's (garbled foreign-script tokens). Neither candidate is currently trustworthy without further fixing; picking between them isn't resolved by this milestone's evidence. Total spend across both benchmark sessions (2026-09-18): **$1.31** of the $10 top-up.
- **Metric-analysis correction (2026-09-19, before the guided-decoding fix below):** initially misread `"ข้าวสวย"`/`"กระเทียมสับ"` flags as `_PANTRY_STAPLES` false positives and proposed widening that allowlist — **wrong, retracted without editing code.** Checked `backend/eval/fixtures/generator_eval_cases.json` directly: neither case's ingredient list includes rice or garlic, and the production prompt's rule 6 explicitly restricts the model to the user's ingredients plus exactly 7 named staples (no rice, no garlic) — a deliberate restriction tied to this project's presence-only fridge design (inventory accuracy, not "what a Thai kitchen typically has"). Widening the metric's allowlist would have made it *more permissive than the system's own actual rule*, hiding genuine rule violations rather than correctly flagging them. `_PANTRY_STAPLES` was left unchanged.
- **Root-cause split and fix: guided JSON decoding (2026-09-19), verified on a live vLLM server before landing in code.** Traced the real benchmark failures to two different causes: (1) decoding-time — the model emitting off-schema scripts (Greek/Arabic/Tamil/Chinese) or leaking raw reasoning text into a JSON string value, neither of which the loose `response_format: {"type": "json_object"}` prevents; (2) instruction-following — the model adding ingredients outside what it was told it may use (a semantic constraint, not something decoding-level constraints can enforce). Tested `response_format: {"type": "json_schema", ...}` with per-field regex `pattern`s (English-only for `recipe_name`/`diet_tags`, Thai+Latin+digits for the Thai-required fields) directly against a live vLLM server, iterating on the exact pattern before writing any code:
  - A plain allowed-character-class pattern **reliably blocked all off-schema scripts** across 2 separate live tests — the severe failure mode from every prior run.
  - A `(?=.*[Thai])` lookahead ("field must contain at least one Thai character") was also tried and found **not reliably enforced** — the identical pattern applied to two sibling fields in the same schema produced inconsistent results (one field honored it, one didn't) — this vLLM version's constrained-decoding backend does not reliably support lookahead assertions in JSON-schema `pattern`. Deliberately not used in the shipped fix.
  - Shipped the character-class-only version to `backend/generators/local_generator.py` (`_RECIPE_JSON_SCHEMA`, confirmed backend edit, 148/148 tests still passing).
- **Full benchmark re-run with the guided-schema fix (2026-09-19, fresh L4 pod):**

  | Candidate | Schema validity (before → after guided JSON) | Hallucination rate (before → after) | Mean latency |
  |---|---|---|---|
  | Gemini (baseline, unaffected by this fix) | 0.33–0.67 | 0.0 | ~2.3–4.6s |
  | Qwen3.5-4B | 0.0–0.33 → **1.0** | 0.67–1.0 → **0.33** | ~13.5s |
  | Qwen3.5-9B (fp8) | 0.67 (unchanged — now consistent across **3** separate runs) | 0.33/1.0/1.0 | ~19.6s |

  4B went from consistently the worst performer to a perfect 3/3 schema-validity run, with its one remaining flagged case a minor typo (`"น้ำมน"`, a truncated `"น้ำมัน"`) rather than a foreign-script failure. 9B's schema validity has now held at exactly 0.667 across three independent runs — a genuinely solid signal — and its severe failure modes (reasoning-leak, foreign scripts) did not recur under the guided schema; its remaining hallucination flags are the *legitimate* "added an ingredient the user doesn't have" violations described above, not decoding corruption.
- **Revised final conclusion for this session's work:** most of what looked like "the local model just isn't good enough" was actually a **fixable decoding-control problem**, not a knowledge/capability gap — exactly the outcome the "try cheaper interventions before fine-tuning" reasoning predicted. **4B now looks like the more practical candidate** (perfect validity, faster, cheaper to serve) contrary to every earlier session's leaning toward 9B. The one remaining real gap — models adding ingredients beyond what they were given — is a genuine instruction-following question decoding constraints structurally cannot fix; it's the most defensible candidate for a future fine-tuning case, but still only backed by n=3 synthetic fixtures, not real usage failure cases, so the plan's fine-tuning precondition still isn't met. Total spend across all benchmark sessions (2026-09-18/19): **$1.55** of the $10 top-up.
- **Retest round confirming the guided-schema fix is stable, not a fluke (2026-09-19, fresh L4 pod, results re-verified by parsing the saved JSON files directly rather than from memory before writing this):**

  | Model | Run | Schema validity | Hallucination rate |
  |---|---|---|---|
  | Qwen3.5-4B | 1st guided run | 1.000 | 0.333 |
  | Qwen3.5-4B | 2nd guided run (retest) | **1.000** | 0.333 |
  | Qwen3.5-9B (fp8) | 1st guided run | 0.667 | 1.000 |
  | Qwen3.5-9B (fp8) | 2nd guided run (retest) | **1.000** | 0.667 |

  **4B: perfect schema validity 2/2 guided runs** — the most confidently-good result of this entire milestone, and now the more practical candidate (faster, cheaper, most reliable). **9B: improved from 0.667 to 1.000 on retest** (4 runs total across the whole session: 0.667/0.667/0.667/1.000) — still trending positive under the guided-schema fix, just with fewer confirming runs than 4B. Neither model's remaining hallucination flags (in this round) are decoding corruption anymore — they're the legitimate "added an ingredient beyond what was given" rule violations already described above, plus one interesting edge case: `"นํ้าเปล่าเล็กน้อย"` was flagged as unknown despite `น้ำเปล่า` being an allowed staple — likely a Thai Unicode normalization mismatch (precomposed vs. combining-mark forms of น้ำ), not a real hallucination; not yet fixed, flagged for later.

- **Safety-relevant finding, verified by parsing every saved benchmark run programmatically (not estimated from memory) — production-relevant, not just a benchmark curiosity: Gemini fails the shrimp-allergy fixture (`shrimp_allergy_should_avoid_shrimp_and_shrimp_paste`) in 8 of 9 runs across this entire session**, every time for the same reason (`Allergy violation: พบ 'กุ้ง' ในสูตร` — it included shrimp despite the user's stated allergy). By contrast, filtering to only the runs where each local model was actually the one being served (excluding runs where a benchmark row errored out because a different model was running on the pod at the time): **Qwen3.5-4B passed this same case 4 of 5 times**, and **Qwen3.5-9B passed it 3 of 4 times** — and in both local models' one miss each, the failure reason was unrelated to allergy (oil-less stir-fry; malformed instructions), not a missed allergen. **This is a real production concern, not just benchmark noise: `backend/main.py`'s live `/api/generate-recipe-text` endpoint calls Gemini today, for real users, right now** — this specific, repeatable allergy-avoidance failure is exactly what the Allergen Validation contribution's deterministic rule+KG check (`validate_recipe`'s allergy stage) exists to catch downstream, and per this data, does catch every time in this benchmark (that's why Gemini's `schema_validity_rate` scores low here) — but it means the *generator* itself is not reliably respecting an explicit allergy instruction, and every one of those Gemini failures cost a wasted retry cycle. Caveat: this is one fixture/one allergen (shrimp/กะปิ) repeated across runs, not a diverse allergy test set — the signal is strong and consistent for this specific scenario, but should not be generalized to "Gemini is unsafe for all allergies" without testing more allergen scenarios.
  - Total spend across the entire local-LLM-generator benchmark effort (2026-09-18/19, all pods): **$1.66** of the $10 top-up.
- All RunPod pods deleted after use; no pod left running. `backend/.env`'s `LOCAL_LLM_BASE_URL` still points at a now-deleted pod's URL (stale, harmless — next real run needs a fresh pod and a fresh URL there anyway).
- **`run_benchmark.py` extended to save the full generated `recipe` object per case (commit `f9e1fc11`), then a fresh capture run made to let the user personally inspect real recipe text (2026-09-19), not just metrics** — `benchmark-4b-full-recipe.json`/`benchmark-9b-full-recipe.json` (scratchpad, not committed — benchmark output, not project source). Personally verified the Gemini shrimp-allergy finding above directly from real recipe text: Gemini's output still lists `กะปิ` (shrimp paste) as an ingredient and only appends a warning telling the user to check the label themselves, rather than substituting it — confirms the logged finding is real, not a metric artifact.
- **False alarm, retracted: no encoding corruption in the benchmark JSON files.** While inspecting the new full-recipe files via `Bash` + Python `print()` (piped through Git Bash on Windows), Thai text displayed as mojibake (`????`-style boxes) — initially misdiagnosed as a file-save encoding bug and almost logged as one. **Wrong.** Re-read the same files with the `Read` tool (no shell/console in the path) and the text was correct throughout. Root cause: Git Bash's console codepage mangles UTF-8 on **display** via `print()`/stdout piping on this Windows setup — the files on disk were never corrupted. **Correct behavior going forward: never diagnose Thai/non-ASCII text problems in a saved file via `Bash`+Python `print()` on this machine — use the `Read` tool, or if a script must inspect text, dump `[hex(ord(ch)) for ch in s]` to a file and check codepoints directly (bypasses console rendering entirely).** Logged to `MEMORY.md`.
- **Real finding upgraded from the retest round's parked note (line 148 above): the allergy-avoidance metric has a bigger gap than "Unicode normalization noise" — and it's a live `/backend/main.py` production bug, not just an eval-side metric limitation.** Re-inspecting the corrected (non-corrupted) 9B full-recipe file found a `shrimp_allergy_should_avoid_shrimp_and_shrimp_paste` case that scored `valid: true, reason: null` while the recipe still contained `กะปิ` (shrimp paste) as a plain ingredient with no hedge/warning text at all — unlike the Gemini case, which only got caught because its warning sentence happened to contain the literal word "กุ้ง". Traced it: `eval/metrics.py`'s `check_schema_and_constraints` calls `main.py`'s real `validate_recipe()` → `check_allergy()` read-only — so this is the exact same allergy check the live `/api/generate-recipe-text` endpoint runs for real users today. `ALLERGEN_MAP["shrimp"]["blocks"]` was `["shrimp", "prawn", "กุ้ง"]` — a literal substring match on the allergen word itself, no mapping from a known derived-ingredient name (`กะปิ`) to the allergen it's made from.
  - **Fixed as an interim patch (2026-09-19, commit `b1fd6293`, confirmed `/backend/main.py` edit): added `"กะปิ"` to the shrimp `blocks` list.** Verified directly (not just via test count): `check_allergy()` on `{"adjusted_ingredients": ["หมูสับ 200 กรัม", "กะปิ 1 ช้อนโต๊ะ"]}` with `{"allergy": "แพ้กุ้ง"}` now returns a violation (previously passed). 148/148 backend tests still passing.
  - **Explicitly not the real fix — do not conflate the two.** This project's Allergen Validation contribution design (line 33-34 above) already planned a proper Knowledge Graph layer (transitive derived-ingredient detection, e.g. `กะปิ→กุ้ง`, real node/edge/traversal — not a flat tag list) specifically for this class of gap; it is not built yet. The one-line patch above only covers the single entry this benchmark happened to surface (`กะปิ`) — it does not generalize to `น้ำปลากุ้ง`, `ผงชูรสกุ้ง`, or any other derived ingredient not manually added to the list. Applied now because it's a real, live safety gap affecting real users with an unclear KG timeline, not because it substitutes for the KG contribution — **never report this patch as "Allergen Validation Knowledge Graph implemented."**
- Same 9B file (verified via `Read` tool, confirmed not a display artifact) also showed the model inventing ingredients absent from the case's input list in 3/3 cases (`ข้าวสวย`, `น้ำมะนาว`/`ซอยหอมแดง`, `กระเทียมสับ`) and, separately, occasionally generating a malformed/nonsense Thai word (`"ไข่ง่า"` instead of `"ไข่ไก่"`, `"เปร็กรากาด"`) — confirmed via raw Unicode codepoint dump (`[hex(ord(ch)) ...]`, bypassing console rendering) that these are genuine valid-Thai-character sequences forming non-words, not corrupted bytes. Both are real generator-quality signals distinct from the guided-JSON decoding fix (which only constrains *script*, not spelling or instruction-following) — consistent with the already-logged "remaining hallucination flags are legitimate instruction-following gaps" conclusion, just with a starker example.
- **Independent recapture run (2026-09-19, fresh L4 pod `uab47ct8vfxmbo`, `$0.49/hr`, same fp8/3072-context 9B config) — done specifically to (a) confirm the retracted "encoding bug" theory with a clean re-run and (b) get the new `input` field into a real capture.** Inspected every result exclusively via `Read`/`Grep` this time, never `Bash`+`print()`, per the newly-logged MEMORY.md gotcha. Result: **byte-for-byte identical output to the previous (already-corrected, non-corrupted) 9B capture** — same latencies (15.813s/20.859s/21.672s), same `recipe_name`s, same malformed words (`"ไข่ง่า"`, `"เปร็กรากาด"`), same `กะปิ`-without-warning shrimp case. This is expected (`LocalLLMGenerator` calls vLLM at temperature 0 / deterministic) and doubles as independent confirmation: the earlier file was never corrupted, and the malformed-word / missed-derived-allergen findings are reproducible, not one-off sampling noise. 4B recapture (schema validity 1.0, hallucination 0.33) also matched prior guided-schema numbers. Both new files (`benchmark-4b-recapture.json`, `benchmark-9b-recapture.json`, scratchpad, not committed) now carry the `input` field (real per-case `ingredients`/`user_prefs`/`base_recipe`, not a template) alongside `recipe`, so any future finding can be verified against exactly what was fed in without opening the fixtures file separately. Pod deleted immediately after, confirmed via `list-pods` (empty). Total additional spend: well under $0.50 (one L4-hour).
- **Eval fixtures expanded 3 → 8 cases (2026-09-19, commit `70424c02`, confirmed `/backend/` edit) — the precondition both the user and this session had agreed on before seriously considering fine-tuning.** Scoped narrowly per the Hard Rule against opening a 4th contribution: only exercises allergens `ALLERGEN_MAP` already supports (peanut, milk, egg, gluten, a second peanut-derived case), no new allergen categories, no new code paths. Each new case's expected `check_allergy()` outcome was **verified empirically by calling it directly before committing**, not assumed:
  - `peanut_allergy_should_avoid_peanut`, `egg_allergy_should_avoid_egg`: straightforward true-positive violations.
  - `milk_allergy_coconut_milk_should_not_false_flag`: false-positive trap (`กะทิ`/coconut milk is not dairy) — verified it correctly passes (`check_allergy` → `("OK", True)`).
  - `gluten_allergy_soy_sauce_known_gap` (`ซีอิ๊วขาว`), `peanut_allergy_satay_sauce_known_gap` (`น้ำจิ้มสะเต๊ะ`): known-gap cases mirroring the `กะปิ` finding — verified both slip through `check_allergy()` undetected, same class of gap the planned Allergen Validation Knowledge Graph exists to close. **First attempt at a second case used `น้ำปลากุ้ง` (fish sauce made with shrimp) as a shrimp-derived known-gap analog — wrong, caught by direct verification before committing: it contains the literal substring `กุ้ง` so `check_allergy()` correctly blocks it, not a gap.** Replaced with the peanut-satay-sauce case instead.
  - 148/148 backend tests still passing after the expansion.
- **First real benchmark run against the expanded 8-case set (2026-09-19, fresh L4 pod `j8sawvtp8xg7wj`, `$0.49/hr`) — surfaced 2 new real `check_allergy()` false-positive bugs and a large quality drop for both local models vs. the old n=3 set:**

  | Candidate | Schema validity (n=3 → n=8) | Hallucination rate (n=3 → n=8) | Mean latency |
  |---|---|---|---|
  | Gemini (run paired w/ 4B) | — | 0.625 | 0.0 | 3.1s |
  | Gemini (run paired w/ 9B) | — | 0.875 | 0.125 | 2.9s |
  | Qwen3.5-4B | 1.0 → **0.625** | 0.33 → **0.75** | 15.0s |
  | Qwen3.5-9B (fp8) | 0.667–1.0 → **0.375** | 0.33–1.0 → 0.75 | 21.3s |

  **Both local models score notably worse on the wider set — real evidence the n=3 numbers were an optimistic small-sample read, exactly the risk expanding the eval set was meant to catch.** But the raw numbers above overstate the drop somewhat: **2 of the "failures" turned out to be metric/check false positives, not real generator failures**, found by reading the actual `reason` strings and recipe text (not assumed):
  1. **`ALLERGEN_MAP["peanut"]` and `["soy"]` both listed the generic word `"ถั่ว"` as a trigger** — so a user stating `"แพ้ถั่ว"` (peanut allergy, the common colloquial meaning) also got checked against soy's blocks. Gemini's peanut case (run paired w/ 4B) was wrongly blocked: `"Allergy violation: พบ 'ซีอิ๊ว' ในสูตร (ผู้ใช้แพ้ soy)"` — the recipe had soy sauce, not peanuts, and the user never said they're allergic to soy. **Fixed (2026-09-19, commit `9f02089c`, confirmed `/backend/main.py` edit): removed `"ถั่ว"` from soy's triggers, kept under peanut only.** Verified directly: soy-sauce-only recipe under peanut allergy now passes; real peanut and explicit-soy (`"ถั่วเหลือง"`) cases still correctly block. 148/148 tests still passing.
  2. **Substring match on the full ingredients text also catches negation phrasing.** Qwen3.5-9B's milk case correctly avoided dairy and added a clarifying note — `"กะทิ (สูตรไม่มีนมวัว/ของแท้)"` ("no cow's milk") — but got wrongly blocked: `"Allergy violation: พบ 'นม' ในสูตร (ผู้ใช้แพ้ milk)"`, because `check_allergy`'s substring match found `"นม"` inside `"ไม่มีนมวัว"`. **Deliberately not fixed** — considered a negation-aware regex heuristic (e.g. `"ไม่มี" + allergen` → treat as safe) and rejected it: it risks introducing false *negatives* (missing a real violation sitting near unrelated negation text elsewhere in the same string), which is a worse failure direction for a safety check than the over-blocking it would fix. Logged as a known limitation instead, same treatment as the existing Thai Unicode-normalization entry.
  - The known-gap cases behaved exactly as predicted for both local models: `gluten_allergy_soy_sauce_known_gap` and `peanut_allergy_satay_sauce_known_gap` both passed `check_allergy()` undetected for Gemini and 9B (4B's gluten case separately failed on an unrelated `"Invalid instructions"` schema issue, not an allergy miss) — consistent, reproducible evidence for the planned Allergen Validation Knowledge Graph's motivation, not one-off noise.
  - Also newly visible at n=8: 4B's output quality degrades much more on recipe types outside the original 3 fixtures' base dishes (Larb Moo, Gaeng Kiew Wan Gai, Moo Satay) — noticeably more garbled English/nonsense-word leakage (`"Febru่"`, `"society"`, `"DLL"`, `"Portsmouth"`) than seen on the narrow set, even with the guided-JSON schema fix still in place (it constrains script, not word validity — consistent with the already-logged malformed-word finding).
  - Pod deleted immediately after both runs, confirmed via `list-pods` (empty). Total additional spend: ~$0.49 (under 1 GPU-hour).
- **Retest with the `ถั่ว`/soy fix in place (2026-09-19, fresh L4 pod `fhayrru97yp0d2`, `$0.49/hr`) — done specifically to get a corrected number set, not to hunt for new findings.**

  | Candidate | Before fix | After fix |
  |---|---|---|
  | Gemini (run paired w/ 4B) | 0.625 / 0.0 halluc | **0.875** / 0.0 halluc |
  | Gemini (run paired w/ 9B) | 0.875 / 0.125 | **0.875** / 0.0 halluc |
  | Qwen3.5-4B | 0.625 / 0.75, 15.0s | 0.5 / 0.75, 17.0s |
  | Qwen3.5-9B (fp8) | 0.375 / 0.75, 21.3s | **0.375** / 0.75, 18.1s (unchanged) |

  Read per-case, not just the aggregate: **Gemini improved cleanly on both runs** (the soy false positive on its peanut case is gone, verified by reading `reason` — both peanut cases now `null`/pass). **9B's numbers are identical before/after** — expected, since 9B's own peanut-case failure (`"พบ 'ถั่วลิสง' ในสูตร"`) was always a *real* violation (the model actually included peanuts), not the soy bug; its milk case still fails on the deliberately-unfixed negation false positive, exactly as predicted. **4B dropped further (0.625 → 0.5)** — not a regression from the fix, traced to two new-this-run `"Stir-fry without oil"` failures (an unrelated instruction-following issue, same failure text seen once before in the pre-fix run's Gemini/9B blocks) — a reminder that n=8 is still small enough for run-to-run variance in what specifically fails, even though the *fixed* bugs behaved exactly as expected. The known-gap cases (gluten, peanut-satay) passed undetected again on both models, in both runs — 4 for 4, reinforcing that this is a consistent pattern, not noise.
  - Pod deleted after, confirmed via `list-pods` (empty). Total additional spend: ~$0.49.
  - **This is now the current, corrected 8-case baseline.** Any future comparison (further fixes, fine-tuning evidence, latency-acceptance decision) should read from this retest, not the earlier pre-fix numbers above.

- **Lightweight prompt-only validation before committing to fine-tuning (2026-09-19/20, fresh L4 pod `chmrw7xyeu9nc6`, `$0.49/hr`) — done specifically to test whether the two "looks-trainable" failure categories (explicit-allergen inclusion, stir-fry-without-oil) respond to a cheap prompt fix before spending on real fine-tuning, per the DISSENT raised when the user proposed accepting worse latency in exchange for training-driven quality.** Added local-model-only rules 8-9 to `prompts.py` (commit `933a158c`) and re-ran the 8-case set.

  | Candidate | Before prompt fix | After prompt fix |
  |---|---|---|
  | Gemini (unaffected, control) | 0.875 / 0.0 halluc | 0.875 / 0.0 halluc |
  | Qwen3.5-4B | 0.5 / 0.75, 17.0s | 0.75 / 0.625, 14.5s |
  | Qwen3.5-9B (fp8) | 0.375 / 0.75, 18.1s | 0.625 / 1.0, 19.4s |

  **Read past the aggregate — the two models' improvements are not equally trustworthy:**
  - **4B's number is confounded, not a clean win.** Read the actual `reason`/recipe text: both new `"Stir-fry without oil"` fails had oil in the recipe, just misspelled (`"น้ำม้น"`, `"น้ำมื่อ"` instead of `"น้ำมัน"`) — a false negative from `check_logic`'s exact-substring match. The one shrimp-allergy case that newly passed did so because the model spelled `กะปิ` as `"กะperi"` and swapped in an entirely different, unrelated recipe — a false negative from `check_allergy`, not genuine allergen avoidance. **4B's Thai spelling quality was visibly worse this run than in prior runs** (garbled words even inside otherwise-correct sentences), which is enough on its own to move both check outcomes in either direction — the "improvement" cannot be attributed to rules 8-9 with this run alone.
  - **9B shows a genuine, verified partial win.** `peanut_allergy_should_avoid_peanut` and `egg_allergy_should_avoid_egg` both now `valid: true` with the actual allergen fully absent from `adjusted_ingredients` (not hidden by misspelling) — read directly, real behavior change. But two other cases got *worse in substance* while incidentally being caught: `peanut_allergy_satay_sauce_known_gap` explicitly wrote `"ถั่วลิสง"` and recommended it as a garnish for a peanut-allergic user (this run only passed the *check* by coincidentally spelling the allergen out; prior runs of the same "known gap" case evaded detection by *not* spelling it out — the case's outcome depends on incidental wording, not a stable underlying behavior). `milk_allergy...`'s new `"Stir-fry without oil"` failure is a *legitimate* `check_logic` catch this time (no oil at all in a coconut-milk curry that loosely used the word `"ผัด"`) but never reached `check_allergy` to check for real dairy — see the pipeline-ordering finding below.
  - **Net read: prompt-only rules had a real, partial, non-zero effect on 9B for the categories they targeted, but the "known gap" cases are not a stable metric signal (their pass/fail flips on incidental wording choices, independent of any actual improvement) and n=8 with single runs per condition is still too small/noisy to treat any of this as strong evidence for or against fine-tuning.**
  - **Separate, more actionable finding — not about the model at all:** `main.py`'s `validate_recipe()` 6-step pipeline stops at the first failing check. `check_logic` (step 3, a quality check) runs *before* `check_allergy` (step 5, a safety check) — so whenever a recipe fails step 3 for an unrelated reason (like the loose-`"ผัด"`-wording case above), nothing downstream is ever evaluated, meaning **a real allergy violation could be silently sitting in a recipe that already failed for a different reason, and the system never finds out.**
  - Pod deleted after both runs, confirmed via `list-pods` (empty). Total additional spend: ~$0.49.

- **Pipeline-ordering fix (2026-09-20, commit `ef865475`, confirmed `/backend/main.py` edit): moved `check_allergy` to run right after `check_ingredients`, ahead of `check_flavor`/`check_logic`/`check_nutrition`, in `validate_recipe()`'s 6-step pipeline.** No test asserted a specific ordering among the 6 stages (only that shape-validation runs before all of them), so the reorder was safe to make directly. Verified empirically, not just by test count: constructed a recipe with both a real dairy allergen (`ครีม`, milk allergy) and an unrelated logic failure (stir-fry instructions with no oil) — before this fix that would have reported `"Stir-fry without oil"` (masking the allergy issue entirely, per the finding above); after the fix it correctly reports the allergy violation. 148/148 backend tests passing. This is a deliberate behavior change to `/api/generate-recipe-text`'s live retry-feedback loop: when a recipe has multiple simultaneous problems, the LLM's next-attempt feedback now surfaces the safety issue first rather than whichever quality issue happened to be checked first.
  - **New gap surfaced while verifying this fix (flagged, not fixed — out of scope for this task):** `ALLERGEN_MAP`'s milk category blocks `"cheese"` (English) but not its common Thai transliteration `"ชีส"` — same class of gap as the `กะปิ` derived-ingredient issue, a different specific term. Logged in `MEMORY.md`.
  - **Fixed immediately after (2026-09-20, commit `1be328b5`, confirmed `/backend/main.py` edit): added `"ชีส"` to milk's triggers/blocks.** Verified directly: a recipe containing `ชีส` under a milk allergy now blocks. 148/148 tests passing.

- **Final retest with every fix from this session in place (`กะปิ`, `ถั่ว`/soy, pipeline-ordering, `ชีส`) — 2026-09-20, pod `y6xisx2eoo3gfd` (EUR-IS-1, `$0.49/hr` — two earlier attempts in `EU-RO-1`, `rud577sfrl0ash` and `7jv7c0sfgqc4h2`, sat with no `runtime` info for 2+ minutes each and were deleted without being billed for GPU time beyond provisioning; switching data center to `EUR-IS-1` via `dataCenterIds` resolved it immediately):**

  | Candidate | Original baseline | After `ถั่ว` fix | After prompt-only fix | **Final (all fixes)** |
  |---|---|---|---|---|
  | Gemini | 0.625–0.875 / 0.0–0.125 halluc | 0.875 / 0.0 | 0.875 / 0.0 | **0.875 / 0.0**, ~3.0s |
  | Qwen3.5-4B | 0.625 / 0.75, 15.0s | 0.5 / 0.75, 17.0s | 0.75 / 0.625, 14.5s | **0.625 / 0.625**, 16.4s |
  | Qwen3.5-9B (fp8) | 0.375 / 0.75, 21.3s | 0.375 / 0.75, 18.1s | 0.625 / 1.0, 19.4s | **0.75 / 0.875**, 19.2s |

  **9B improved monotonically across every fix round (0.375 → 0.375 → 0.625 → 0.75)** — its final run: `peanut_allergy_should_avoid_peanut` and `egg_allergy_should_avoid_egg` again genuinely omit the allergen (consistent with the prompt-fix run), the milk case passed this time (the deliberately-unfixed negation false positive didn't recur this run — consistent with it being a real but intermittent gap, not fixed, just not triggered here), `shrimp_allergy` still correctly caught (`กะปิ`), and the only failure was `gluten_allergy_soy_sauce_known_gap` — this time for an unrelated reason (`"Frying without oil"`), not a missed allergy — reinforcing that the known-gap cases are a noisy signal case-to-case, not a stable one.
  - **4B landed between its two prior extremes (0.5, 0.75) at 0.625** — consistent with the already-logged conclusion that 4B's schema-validity number this benchmark is dominated by run-to-run spelling-corruption noise, not a clean signal of any specific fix's effect.
  - **This is the current, final baseline for this session's local-LLM-generator work.** Any future decision (latency-acceptance, fine-tuning) should read from this table, not any of the earlier ones above.
  - Total additional spend this final round: ~$0.49 (the two stuck EU-RO-1 pods were deleted before any meaningful GPU-hour accrued). Verified via `runpodctl user`: account balance now **$6.9061764442** of the $10 top-up — total spend across all local-LLM-generator benchmarking (2026-09-18 through 2026-09-20) is **$3.09**, of which **~$1.29** was this session's work (starting balance $8.1957417273, confirmed at the top of this session).

- **Eval fixtures expanded again, 8 → 15 cases (2026-09-20, commit `34242326`), specifically to isolate whether ingredient-hallucination is driven by allergy-avoidance pressure or is a general model habit — the one large remaining gap between Gemini and the local models at n=8.** Added 5 no-allergy cases (no allergy pressure at all, to measure baseline hallucination rate) and 2 allergy cases pairing the allergen with a dish that traditionally contains it (`Tom Yum Goong` + shrimp allergy, `Som Tam` + peanut allergy — tests real substitution behavior, not incidental-ingredient avoidance). Also adds dish-type diversity (soup/ยำ/curry) beyond the previous set's stir-fry-heavy mix. 148/148 tests passing after the expansion.

- **Full n=15 benchmark run (2026-09-20, fresh L4 pod `c8t42t85rwqbm7`, `EUR-IS-1`, `$0.49/hr` — used `dataCenterIds` explicitly this time since two consecutive `EU-RO-1` pods had sat with no `runtime` populated for 2+ minutes each earlier this session and were deleted unused):**

  | Candidate | Schema validity | Hallucination rate | Mean latency |
  |---|---|---|---|
  | Gemini | **0.933** | 0.0 | 2.2–2.6s |
  | Qwen3.5-4B | 0.6 | 0.733 | 14.8s |
  | Qwen3.5-9B (fp8) | 0.733 | 0.8 | 19.2s |

  Validity/hallucination numbers are consistent with the n=8 final baseline (9B: 0.75/0.875 → 0.733/0.8; 4B still the noisiest of the three, as already established) — the larger n didn't overturn anything, it sharpened the picture.

  **Key finding this round, read directly from the 5 no-allergy cases (no allergy-avoidance motive present at all): both models hallucinated in 4 of 5 of them** (`no_allergy_single_ingredient_baseline`, `no_allergy_many_ingredients_baseline`, and either `no_allergy_soup_dish_type_diversity` or `no_allergy_yum_salad_dish_type_diversity` depending on the model, plus `no_allergy_curry_dish_type_diversity`) — **identical 4/5 rate for both 4B and 9B.** This directly answers the question the expansion was built to answer: ingredient hallucination is **not** primarily an artifact of the model trying to substitute away from a stated allergen — it happens just as often with zero allergy pressure. It's a general instruction-following gap (not reliably respecting the prompt's "use only the given ingredients + the 7 named staples" rule), the same category of failure prompt rule 9 partially improved for explicit-allergen cases in the earlier lightweight validation.
  - **This sharpens the case for a narrowly-scoped fine-tuning target, if training is pursued: reduce ingredient-hallucination specifically, framed as improving the local generator's instruction-following reliability (fits within the existing Critic-Feedback Loop / Allergen Validation contribution framing — not a new 4th contribution) rather than "make the local model better than Gemini overall."** Latency (Gemini ~2.4s vs. local 15–19s) remains unaffected by any of this and is a separate, still-unresolved acceptance question.
  - Pod deleted after both runs, confirmed via `list-pods` (empty). Additional spend this round: **~$0.23** (verified via `runpodctl user`: balance $6.9061764442 → $6.6755226636). Running total: **$3.32** of the $10 top-up.

**Local LLM Fine-Tuning — dataset-construction pipeline (2026-09-21, worktree `worktree-local-llm-finetune`, 6-task SDD plan, all tasks done, 1 final-review fix wave, merged to `main` local — same "Critic-Feedback Loop / Allergen Validation instrumentation" framing as the generator work above, still not a 4th contribution):**
- Advisor's training-component requirement now has enough evidence to act on (per the n=15 finding above: hallucination is a general instruction-following gap, not allergy-avoidance-driven — the one clearly-scoped, defensible fine-tuning target). Full evidence trail written up as a standalone reference doc for the advisor: [`docs/LOCAL_LLM_EVIDENCE_LOG.md`](docs/LOCAL_LLM_EVIDENCE_LOG.md) (gitignored `docs/`, not tracked — local reference only, per explicit user request not to track it).
- Design: `docs/superpowers/specs/2026-09-21-local-llm-finetune-design.md`; Plan: `docs/superpowers/plans/2026-09-21-local-llm-finetune.md` (both gitignored `docs/`, present in this checkout only).
- **Advisor review before writing the plan caught 4 real gaps in the initial design**, all resolved before implementation: (1) eval-contamination risk — training on a dish that's also an eval fixture's `base_recipe` would measure memorization, not generalization; resolved with a dish-family exclusion matcher, verified to catch both real near-duplicates found by direct file inspection (`ส้มตำแตงร้าน`/Som Tam, `ยำปลาหมึกสด`/Stir-Fried Squid). (2) `nutrition` field has no deterministic source — `IngredientNutrition` table exists in the schema but has zero rows and no code path uses it (confirmed by direct check, not assumed); resolved by masking `nutrition` from the training loss entirely, not fabricating values. (3) Naive "drop 1-2 ingredients from a real recipe" sampling never produces the sparse-input shape (1-2 given vs. a full dish) where hallucination actually occurs; resolved with a two-tier design — Tier A (near-complete, full supervision) + Tier B (sparse, ingredients-only supervision, ≥50% of the dataset by design). (4) 4B vs. 9B model choice — picked 9B because its benchmark signal has been reliably monotonic all session, while 4B's is confounded by spelling noise (already logged in MEMORY.md); RTX 4090 picked over cheaper L4 for training infra (HIGH vs. LOW RunPod availability, user confirmed the price trade-off explicitly per the standing GPU-cost-confirmation rule).
- **What was built (`backend/finetune/`, new package, 6 tasks, TDD throughout):** a Thai-text combining-mark normalization fix (corrects a previously-wrong MEMORY.md hypothesis — plain `unicodedata.normalize("NFC", ...)` does NOT fix the bug; the real fix decomposes SARA AM and reorders tone marks, verified directly before shipping); Thai ingredient-name extraction + bilingual dish-family matching helpers; a recipe-source loader for the `pythainlp/thai_food_v1.0` xlsx dataset (146/159 rows usable, with markdown-fallback parsing for rows missing structured columns); the eval-fixture exclusion matcher; Tier A/Tier B SFT example builders with **real prompt parity** to `LocalLLMGenerator` (imports `build_recipe_prompt(..., include_example=True)` directly, not a reimplementation); and the final dataset-assembly script enforcing the Tier B ≥50% floor in code (capping Tier A, never fabricating Tier B).
- **Real-data verification caught 2 more implementation-level bugs no synthetic test could have found** (Task 2's implementer, then Task 5's): (a) the original `dish_families_overlap` design used single-language (Thai-only) keyword matching, which can never match against the eval fixtures' English/transliterated names (`"Som Tam"` etc.) — the plan was corrected mid-implementation to bilingual `(thai_form, english_form)` pairs, checked in either name order. (b) Task 5's implementer verified the instruction-clause-splitting heuristic against all 146 real recipes (not just the plan's one synthetic test) and found it would have shredded real instructions into single words/digits via an unsafe fallback, and separately leaked a dropped ingredient's name into 46/217 (21%) of real Tier A examples — fixed by broadening Thai clause-connector boundaries and having the splitter return `None` (masked from the loss, same contract as Tier B) whenever a leak-free split can't be guaranteed, rather than ever shipping unsafe training data.
- **Final whole-branch review (opus) ran the real pipeline end-to-end and the full test suite independently** (not just read the diff) — confirmed all cross-task interfaces line up, `nutrition` is genuinely never touched, and both real eval-fixture near-duplicates are excluded. Caught 2 Important findings, both documentation gaps rather than code defects: (1) the target-sequence/loss-mask construction for the actual training step was never written down, despite determining whether the eventual fine-tune passes its own schema-validity regression guard — now documented as design spec §4.6, an explicit open item to resolve before training (recommending fabricate-and-mask placeholder values for the 5 unsupervised schema fields as the safer default, not decided as final). (2) The exclusion list itself was never recorded anywhere auditable, despite being an explicit spec §3.1 requirement — fixed, both in the design spec and by having `build_dataset.py` print the excluded names. Also fixed in the same wave: a `ZeroDivisionError` guard, deduplication (4/473 real examples were exact duplicates from a genuinely duplicated source row), and a cross-reference comment noting the pantry-staple list now exists as 3 independently-synced copies across the codebase. One residual Minor parked (not blocking): the dedup step runs after the Tier B floor cap rather than before, so the floor is only pre-dedup-guaranteed — immaterial at the real duplicate rate (0.8%), flagged for a future re-run at larger scale.
- **Real-run output (2026-09-21, `pythainlp/thai_food_v1.0` xlsx + the 15-case eval fixtures): 469 training examples after dedup (200 Tier A, 269 Tier B, 57.4% Tier B — floor satisfied), 10 recipes excluded for eval-fixture overlap.** Dataset written to `finetune_dataset.jsonl` (gitignored, not committed — generated artifact).
- 182/182 backend tests passing on `main` after merge (148 pre-existing + 5 from `eval_metrics`/Task 1 + 29 new across the `finetune/` package's own test files).
- **Still deferred, unchanged:** actually running QLoRA training on a RunPod GPU pod, re-benchmarking the fine-tuned model back-to-back against a freshly-served baseline (design spec §6.2's noise-floor rule: nothing under a 3-case/0.2 shift on the 15-case eval counts as a real effect), and resolving §4.6's target-sequence/loss-mask decision before that training run starts. This is the "Post-Plan Operational Runbook" the plan document itself defers to real infrastructure work, same split Milestone 1 used.

**Auth Phase 3 — real backend-verified OTP password reset, replacing the fully-mocked flow (2026-09-12, worktree `auth-phase3-otp-reset` branch `worktree-auth-phase3-otp-reset`, 8-task SDD plan, all tasks done, 1 final-review fix wave) — product envelope (Auth), ไม่ใช่ contribution หลักที่ต้องมี evaluation:**
- Spec: `docs/superpowers/specs/2026-09-12-auth-phase3-otp-reset-design.md` (local/ignored — `docs/` is gitignored, not present in this worktree checkout, only in the main checkout); Plan: `docs/superpowers/plans/2026-09-12-auth-phase3-otp-reset.md`; ledger: `.superpowers/sdd/2026-09-12-auth-phase3-otp-reset/progress.md`
- **What was built:** `requestOtp`/`resendOtp`/`verifyOtp`/`resetPassword` in `AuthContext.jsx` were fully client-side mocks before this phase (no backend OTP code existed at all). Now real end-to-end: new `password_reset_otps` table + `backend/otp.py` (generation/hashing/verification helpers) + three real endpoints — `POST /api/auth/forgot-password`, `POST /api/auth/verify-otp`, `POST /api/auth/reset-password`. **Correction to avoid overclaiming (checked `backend/email_sender.py` directly):** delivery is NOT real SMTP — `send_otp_email` is a deliberate dev-mode stub that only `logging.info`s the code server-side; the module's own docstring says swapping in real SMTP later is confined to that one function's body plus new env vars, no caller needs to change. So "no email is actually sent yet" — the OTP/ticket/reset logic downstream of it (hashing, attempt cap, cooldown, ticket consumption, transactional reset) is fully real, only the delivery transport is still a stand-in.
- **Key security properties:** OTP codes are hashed with **HMAC-SHA256 keyed by `OTP_HMAC_SECRET`** (`otp.hash_otp_code`), not plain SHA-256 — the 6-digit keyspace (1,000,000 values) is small enough to brute-force offline from a leaked hash, so a keyed hash is load-bearing, not decorative (`test_hash_otp_code_is_deterministic_and_keyed` now actually asserts this against an unkeyed SHA-256 comparison). Reset tickets are plain SHA-256 (safe — high-entropy `secrets.token_urlsafe(32)` token, not a small keyspace). All state transitions (`create_otp_for_user`, `attempt_verify_otp`, `consume_reset_ticket`) use **atomic compare-and-set `UPDATE ... WHERE ... RETURNING`**, not read-then-write checks, so they're race-safe under concurrent requests. `forgot-password` and `verify-otp` return **identical generic responses** for known/unknown emails and right/wrong codes (non-enumeration). `reset-password` does the ticket-consume + password-hash-update + all-refresh-tokens-revoke in **one transaction** (single commit) so a failure partway through can never leave a burned ticket with the old password still active, or a new password with old sessions still valid.
- **Final whole-branch review (opus, range `c721f67b..d5643cc6`) caught and fixed 1 Critical + 3 Important before merge** — the exact kind of composability bug per-task review can't see (each task only reviews its own diff):
  1. **Critical, fixed in this wave: the 5-attempt cap was bypassable via resend, defeating OTP brute-force protection entirely.** `otp.get_active_otp_created_within`'s cooldown-check predicate required `consumed_at IS NULL AND expires_at > now` — but the 5th-wrong-attempt path in `attempt_verify_otp` deliberately sets `expires_at = now` to invalidate the row (that invalidation itself is correct and unchanged). Consequence: once a row is invalidated by hitting the attempt cap, it became invisible to the cooldown check, so an immediate `forgot-password` resend issued a brand-new OTP with `attempts` reset to 0 and no cooldown delay — attacker loop: request OTP → 5 wrong guesses → immediate resend (bypasses cooldown) → repeat, no time bound on guessing at all. Fixed by changing the predicate to depend **only on `created_at`** (renamed `get_active_otp_created_within` → `get_recent_otp_request` since "active" no longer describes what it checks — see the function's docstring in `backend/otp.py`). Pinned with a new regression test, `test_attempt_cap_bypass_via_resend_is_blocked` (`backend/tests/test_password_reset_endpoints.py`) — empirically confirmed it fails against the old predicate (2 rows created instead of 1) and passes against the fix, by temporarily reverting the predicate, re-running the test, and restoring the fix.
  2. Important, fixed: `OTP_HMAC_SECRET` was a new required env var completely undocumented in `README.md` — backend wouldn't boot for anyone pulling `main` without already knowing this secret was required. Added, matching the exact format/hint style of the existing `JWT_SECRET_KEY` entry.
  3. Important, fixed: `backend/otp.py` never called `load_dotenv()` itself — worked only by import-order luck (`main.py` imports `auth`, which calls `load_dotenv()`, before it imports `otp`). Any other entry point importing `otp` directly would have crashed. Fixed by adding the same `from dotenv import load_dotenv` / `load_dotenv()` pattern `auth.py` uses.
  4. Important, fixed: the plan's own Testing section named "ticket expiry" and "cross-user ticket rejection" as required coverage; neither existed. Added `test_consume_reset_ticket_fails_when_ticket_expired` (DB-level, `test_otp_db.py`, backdates `ticket_expires_at` directly) and `test_reset_ticket_resolves_only_to_its_own_user_not_another` (endpoint-level, `test_password_reset_endpoints.py`, two registered users, confirms user A's ticket resets only A's password — the load-bearing assertion is that user B's login with B's original, untouched password still succeeds afterward; the accompanying "B's login with A's new password fails" check is trivially true by construction and only rounds out the picture). A third named test, "concurrent-verify race safety" (real multi-threaded), stays explicitly deferred — flagged with a one-line comment in `test_otp_db.py` (genuinely hard to force under SQLite+StaticPool's single shared connection) rather than silently missing.
  - Bundled cheap fixes from the same review's ledger triage: unused `PasswordResetOtp` import removed from `backend/main.py`; unused `import time` removed from `backend/tests/test_otp_db.py`; `frontend/src/pages/auth/OtpVerify.jsx`'s `handleResend` now captures `resendOtp()`'s `{success, error}` result and surfaces a failure via the existing `setError` state instead of silently starting the 60s cooldown lockout on a failed network call.
  - **Not done in this wave (out of the fix-wave's given scope, flagged for the user):** the ledger's final-review note also asked to update `docs/superpowers/specs/2026-09-12-auth-phase3-otp-reset-design.md`'s cooldown-predicate description to match the fix — `docs/` is gitignored and not present in this worktree checkout (only in the main checkout), so this file could not be reached from here; needs a manual edit in the main checkout.
- **Verified:** full backend suite `python -m unittest discover -s tests -v` → **121/121 passing** (117 pre-existing at start of this wave + 4 new: `test_attempt_cap_bypass_via_resend_is_blocked`, `test_get_recent_otp_request_still_blocks_after_attempt_cap_invalidation`, `test_consume_reset_ticket_fails_when_ticket_expired`, `test_reset_ticket_resolves_only_to_its_own_user_not_another`; plus 1 pre-existing test strengthened with an extra assertion — keyed-hash-vs-plain-SHA256 — and 1 pre-existing test renamed to match the renamed function, neither changing the count); `bun run build` → exit 0, 2190 modules; `bun run lint` → same 9 pre-existing errors (`AddIngredient.jsx`, `CookingPage.jsx`, `RecipeDetail.jsx`, `pages_v2/*` — files this branch never touches), zero new.
- Files touched this wave: `backend/otp.py`, `backend/main.py`, `backend/tests/{test_otp_db.py,test_otp_crypto.py,test_password_reset_endpoints.py}`, `README.md`, `frontend/src/pages/auth/OtpVerify.jsx`, `spec.md` (this entry).
- **งานค้าง (backlog) update:** Auth Phase 1's and Phase 2's entries below both still list "Phase 3: real SMTP-backed OTP for password reset" under **Not done** — the backend OTP/reset logic that item referred to is now done as of this entry (see correction above: delivery transport itself is still the logging stub, not real SMTP); full manual browser click-through of the reset-password flow (register → forgot-password → check server log for the code, since no real email is sent yet → OTP → new password → login) has not been performed in this wave and is left for the user to verify manually, same standing decision as this project's other phases (Playwright not installed).

**Auth Phase 2 — migrate `/api/user-ingredients` off the dev-user bridge (2026-09-08, worktree `auth-phase2` branch `worktree-auth-phase2`, bounded task, 1 review round) — product envelope (Auth), ไม่ใช่ contribution หลักที่ต้องมี evaluation:**
- Scope: only `/api/user-ingredients` (`GET`/`POST`/`DELETE`) — the only remaining endpoint still using `get_dev_user_id`. `/api/recipes` and the ingredient-image endpoints were already correctly public (no user data); `/api/user-preferences` was already built on real auth from day one (User Preferences entry below).
- `backend/main.py`: all 3 routes now take `current_user: User = Depends(get_current_user)` and scope every DB call to `current_user.id`; `POST`/`DELETE` (mutating) gained `Depends(verify_same_origin)` (CSRF), `GET` correctly left ungated (safe method). `get_dev_user_id`/`DEV_USER_EMAIL` fully removed from `main.py` — no dangling references.
- New `backend/tests/test_user_ingredients_endpoints.py` (8 tests) proves the actual point of the migration: two different registered users don't see or can't delete each other's rows — coverage Phase 1's own auth tests never exercised. 401/403 cases covered per route.
- Frontend (`App.jsx`, `AddIngredient.jsx`, `UserIngredients.jsx`): the 3 raw `fetch` calls to `/api/user-ingredients*` switched to `apiFetch` (from `useAuth()`) for 401→refresh→retry — matches the pattern `apiFetch` was built for in Phase 1.
- **Known consequence (flagged before starting, not a bug):** every real logged-in user now gets an empty fridge — the old shared `dev@local` seed data is orphaned/invisible to real accounts.
- Verified: full backend suite 80/80 passing (72 pre-existing + 8 new); frontend `eslint`/`vite build` — same 3 pre-existing errors in `AddIngredient.jsx` (unrelated lines, identical on unmodified `main`), zero new; live HTTP test against real Postgres (`homemade-db-1`, scratch port 8020) — 2 real registered users, confirmed isolated ingredient lists, 401 unauthenticated, 403 without a trusted Origin; test users + rows deleted afterward. Independent review (sonnet): 0 Critical, 0 Important, 2 Minor (one fixed same-branch — missing DELETE-401 test; one deferred — `backend/database/seed_postgres.py`'s docstring still references the now-removed `DEV_USER_EMAIL` bridge, out of scope, flagged for manual cleanup per Hard Rule).
- **Not done:** Phase 3 (real SMTP OTP forgot-password) — `requestOtp`/`resendOtp`/`verifyOtp`/`resetPassword` in `AuthContext.jsx` still fully mocked, backend has zero OTP code.

**User Preferences — onboarding quiz persisted per-user, Profile Preferences rewritten, CreateRecipe defaults (2026-09-08, worktree `user-preferences` branch `worktree-user-preferences`, 6-task SDD plan, all tasks done, 2 fix rounds) — product envelope (Auth: Login/Profile), ไม่ใช่ contribution หลักที่ต้องมี evaluation:**
- Spec: [`docs/superpowers/specs/2026-09-08-user-preferences-design.md`](docs/superpowers/specs/2026-09-08-user-preferences-design.md); Plan: [`docs/superpowers/plans/2026-09-08-user-preferences.md`](docs/superpowers/plans/2026-09-08-user-preferences.md); ledger: `.superpowers/sdd/2026-09-08-user-preferences/progress.md`
- **Data model (Task 1):** new table `user_preferences` (`backend/database/models.py`) — `user_id` is the PK directly (genuine 1:1 with `users`, no surrogate `id`); `age`, `cooking_frequency` scalar columns; `cuisine_preferences`/`dietary_restrictions`/`equipment`/`cooking_goals` use **`sqlalchemy.JSON`, not `postgresql.ARRAY`** — verified empirically during planning that `ARRAY` raises `CompileError` against the in-memory SQLite engine every backend test in this repo uses (`GenerateHistory`'s `ARRAY` columns never hit this because nothing tests that table yet); `JSON` round-trips correctly on both SQLite and Postgres and nothing needs to `unnest()` into these columns the way the personalization queries need `GenerateHistory`'s arrays to.
- **Endpoints (Task 2):** `GET`/`PUT /api/user-preferences` (`backend/main.py`) — first endpoints in this codebase to require real cookie auth (`get_current_user`) from day one, no dev-user bridge, since nothing pre-existing depends on them; `PUT` also carries `verify_same_origin` (CSRF) matching the existing auth-route pattern. `PUT` is a plain upsert keyed on `current_user.id` (via `db.get`), age validated server-side to 1–120.
- **Frontend shared infra (Task 3):** new `frontend/src/constants/preferenceOptions.js` (single source of truth for cuisine/restriction/equipment/frequency/goal option lists + emoji maps, replacing what used to be defined three times with drifted wording across `Onboarding.jsx`/`Profile.jsx`/`CreateRecipe.jsx`) and `frontend/src/components/OptionPicker.jsx` (`OptionChips`/`OptionList`, extracted from `Onboarding.jsx`). Disclosed copy changes: canonical `OTHER_OPTION = "อื่น ๆ"` (space) and `NO_RESTRICTION = "ไม่มีข้อจำกัด"` replace `CreateRecipe.jsx`/`Profile.jsx`'s old `"อื่นๆ"`/`"ไม่มี"`; added back `"แพ้ถั่วเหลือง"`/`"แพ้งา"` (existed in the old local allergy lists, missing from Onboarding's, restored so no allergen coverage regressed); added `EQUIPMENT_NONE`/"หม้อตุ๋น/สโลว์คุก" to the equipment list. `RESTRICTION_ITEMS` carries an `isAllergy` tag per item so `ALLERGY_OPTIONS` (a filtered subset, used only by `CreateRecipe.jsx`) never shows diet-type entries (Vegan/Halal) in a field that feeds `main.py`'s allergen-only `check_allergy()`.
- **Onboarding wiring (Task 4):** `Onboarding.jsx`'s `submitProfile()` stub replaced with a real `PUT` via `useAuth()`'s `apiFetch` (handles 401→refresh→retry already). Best-effort: failure is logged, never blocks navigation to `/home`. Confirmed during planning that `register()`'s auto-login (already existed, chains a real `login()` call after register — not something this plan added) means a session cookie exists by the time Onboarding runs, so no backend/flow change was needed for that.
- **Profile Preferences rewrite (Task 5, largest task):** old bespoke local-only `taste`/`allergies`/`equipment` state (never persisted) fully replaced — fetches `GET /api/user-preferences` on first entering the view, renders all 6 onboarding fields with the same `OptionChips`/`OptionList` components, **auto-saves on every pill toggle** (explicit product decision over an explicit Save button). Age input is a plain number field (1–120), not the onboarding wheel-picker — deliberate simplification for a settings page. All toggle handlers pass the freshly-computed next array as an override into the save call (not the just-set state var, which would still be stale in the same closure tick).
- **CreateRecipe defaults (Task 6):** on mount, fetches saved preferences once; defaults the per-generation "อาการแพ้อาหาร"/"อุปกรณ์ที่มี" pills from `dietary_restrictions`/`equipment` (filtered to `ALLERGY_OPTIONS` for allergies, so Vegan/Halal never leak in); `taste` is never defaulted (always re-picked fresh, explicit decision). Only free-text "other" entries never default-fill (matched only by canonical label, not custom text) — disclosed limitation, not a bug.
- **Real bugs found + fixed during review (not shipped):**
  1. Task 1's autogenerated migration silently bundled an unrelated pre-existing `refresh_tokens` unique-constraint drift fix (dropping a redundant `refresh_tokens_token_hash_key` constraint left over from an earlier migration) into a migration titled "add user_preferences table" — caught because the brief explicitly required reading the autogenerated diff, not trusting it. Removed; the pre-existing drift itself was left alone (out of scope, functionally harmless — the equivalent unique index still enforces uniqueness).
  2. **Plan bug (mine): `EQUIPMENT_NONE` was added to the shared equipment options in Task 3's own code but never imported into `Onboarding.jsx`, and the step-4 equipment toggle's `onToggle` was never updated to pass it as `toggleInList`'s `noneValue`** — meaning the newly-added "ไม่มีอุปกรณ์พิเศษ" pill wouldn't have exclusively cleared other selections in the onboarding wizard itself. Fixed same-task (2-line addendum) after task review, before merge.
  3. **Plan bug (mine): Profile Preferences' "other" free-text `onBlur` handlers (dietary restrictions, equipment) PUT the corrected value to the backend but never called the matching `setState`** — local state stayed on the `OTHER_OPTION` placeholder, so the very next unrelated auto-save (any other pill click) would silently overwrite the backend back to the placeholder, destroying the just-saved custom text. Real, easily-triggered data-loss path, not hypothetical. Fixed in a review fix-round.
  4. **Plan bug (mine, most subtle): CreateRecipe.jsx's default-fill race-condition guard was a no-op** — the brief's own sample code checked `allergies.length === 0`/`equipment.length === 0` inside a `useEffect(..., [])`'s async callback, which is a classic React stale-closure bug: that closure permanently sees the mount-time state (`[]`), never live state, so the guard always evaluated true regardless of any pill clicks that happened before the fetch resolved — exactly the failure mode the brief called "load-bearing, not optional" to avoid. Fixed with the functional `setState` updater form (`setAllergies((prev) => prev.length === 0 ? defaults : prev)`), which reads live state at application time.
- **Verified (controller, post all-tasks):** full backend suite `python -m unittest discover -s tests` → **72/72 passing** (63 pre-existing + 9 new, matches the plan's predicted count exactly); frontend `npm run build` → exit 0, 2187 modules (2185 baseline + 2 new files); `npm run lint` → 9 pre-existing errors, all in files this plan never touched (`CookingPage.jsx`, `RecipeDetail.jsx`, `pages_v2/*` — a teammate's unused design mockup files), zero new; live HTTP pass against the real Postgres container (`homemade-db-1`, port 8010 scratch to avoid clashing with a dev backend on 8000): register→login→`GET` (null)→`PUT` (Thai-text payload)→`GET` again, response echoed the exact 6 contracted keys with Thai text intact both ways; test user + its rows deleted from Postgres afterward.
- **Not done / explicitly out of scope for this plan:** full manual browser click-through (Playwright still not installed, same standing decision as Auth Phase 1's Task 8 — **user to verify manually**: register → onboarding all 6 steps incl. age → home → Profile → Preferences shows what was submitted → edit a pill → reload → confirms it persisted → CreateRecipe shows the same allergy/equipment pre-selected, taste unselected); `favorites`/`ratings`/`generate_history` endpoints still don't exist (separate future work, tables have existed since the Postgres migration); `ingredient_nutrition`/`allergen_edges` still empty (Nutrition Engine / Allergen Knowledge Graph, separate academic-contribution track).
- **Final whole-branch review (post all 6 tasks, model: opus) found + fixed 1 more Critical + 3 Important the per-task reviews couldn't see (each task only had its own diff):**
  1. **Critical (mine): `Profile.jsx`'s fetch-on-entry effect marked itself "loaded" even when the GET failed** (no `catch`, `finally` always ran) — since save is a full-replacement `PUT`, the next pill click after a failed load would have silently wiped the user's entire saved row, including their allergy list. Fixed: only mark loaded on success, add a `catch`, and gate `savePreferences` on `prefsLoaded` so a save can't fire before a successful load.
  2. **Important (mine, safety-adjacent): a saved custom "other" allergy/equipment string became invisible and uneditable in Profile after a reload** — it matched no pill so nothing rendered, and the free-text input (gated on selecting "อื่น ๆ") never appeared, while the string kept silently round-tripping on every save. Fixed: on load, split out any entry that doesn't match a canonical option into the "other" text field and re-select "อื่น ๆ" so it's visible/editable again.
  3. **Important (mine): Onboarding save failure was still completely invisible** — `apiFetch` resolves (doesn't throw) on non-2xx, so only network errors ever reached the log; an HTTP error status (e.g. an expired/never-established session, since `/onboarding` isn't `ProtectedRoute`-gated) was silently lost, contradicting the design spec's explicit requirement to surface `stepError`. Fixed: check `res.ok`, set `stepError` on failure, still navigate to `/home` either way (per spec, non-blocking).
  4. **Important: a comment in `preferenceOptions.js` overclaimed backend behavior** — said the allergy-only option list "feeds `check_allergy()`," but that function reads a request field named `allergy` (singular) while every frontend caller sends `allergies` (plural), so it never actually runs on this data (**pre-existing bug, outside this branch's scope, not fixed** — only the misleading comment was corrected, per the project's own MEMORY.md rule against describing mechanisms the code doesn't execute).
  - **Design-scoping error also found + corrected in the spec doc (not code):** the design spec claimed option lists were duplicated "three times" (Onboarding/Profile/CreateRecipe) — missed a **fourth** live copy, `CustomCookingPage.jsx`, which this branch never touched. Net effect: the app now presents two different vocabularies for the same fields depending on which page the user is on (3 of 4 pages agree, 1 doesn't) — worse than before in that specific sense. **Filed as a follow-up** (point `CustomCookingPage.jsx` at the same `constants/preferenceOptions.js` + 3-arg `toggleInList`, mechanically identical to what Task 6 did for `CreateRecipe.jsx`) — deliberately not fixed in this branch (SCOPE DRIFT).
  - One remaining parked Minor (not fixed, not blocking): the load effect's cancellation flag isn't re-checked after its second `await`, so rapidly toggling away from and back to the Preferences view mid-fetch could let a stale response briefly overwrite a newer one — narrow race, self-correcting, no data-loss path.
  - Re-verified after the fix wave: backend 72/72 still passing, frontend build/lint still clean (same 9 pre-existing/0-new baseline). Merged to `main` via fast-forward (no conflicts). Worktree + branch (`worktree-user-preferences`) cleaned up.

**Auth Phase 1 — real cookie/JWT auth complete (2026-09-08, worktree `auth-phase1` branch `worktree-auth-phase1`, 8-task SDD plan, all tasks done) — product envelope (Auth: Login/Profile, JWT), ไม่ใช่ contribution หลักที่ต้องมี evaluation:**
- Spec: [`docs/superpowers/specs/2026-09-08-auth-phase1-cookie-jwt-design.md`](docs/superpowers/specs/2026-09-08-auth-phase1-cookie-jwt-design.md); Plan: [`docs/superpowers/plans/2026-09-08-auth-phase1-cookie-jwt.md`](docs/superpowers/plans/2026-09-08-auth-phase1-cookie-jwt.md); ledger: `.superpowers/sdd/2026-09-08-auth-phase1-cookie-jwt/progress.md`
- Backend (Tasks 1-4, commits `947ca365..6fdf3cb6` on this branch): `refresh_tokens` table (rotating, hashed at rest — `token_hash = sha256(raw jwt)`, never stores the raw token); `backend/auth.py` gains `create_access_token`/`create_refresh_token`/`hash_token`/`get_current_user`/`set_auth_cookies`/`clear_auth_cookies`/`rotate_refresh_token`/`revoke_refresh_token`; `backend/csrf.py` new — `verify_same_origin` dependency rejects mutating requests whose `Origin` header isn't the trusted frontend origin; `main.py` wires `POST /api/auth/register`, `POST /api/auth/login`, `POST /api/auth/refresh`, `POST /api/auth/logout` (all four gated by `verify_same_origin`), `GET /api/auth/me` — access token cookie `Path=/api` `Max-Age=1800` `HttpOnly`, refresh token cookie `Path=/api/auth` `Max-Age=2592000` `HttpOnly`, both `SameSite=lax`
- Frontend (Tasks 5-7, commits `a622c0ca..f8dc59d1`): Vite dev proxy + relative `/api` URLs (so cookies stay same-origin in dev); `AuthContext.jsx` rewritten for real cookie-based session — single-flight `/api/auth/refresh` (concurrent 401s coalesce into one refresh call, not N), bootstrap-on-mount via `/me` so a page reload survives; `ProtectedRoute`/`Loading`/`Profile` gate on `authLoading` (not a naive `isAuthenticated` check) and `Profile`'s logout button awaits the real network call before navigating away
- **Task 8 (this entry) — verification + documentation only, no auth-logic code changes.** Two parts to Task 8's original scope; only the first was done here, by explicit user decision:
  1. **Real HTTP-level verification against live Postgres — done.** Started `backend/venv/Scripts/python.exe -m uvicorn main:app` (this worktree's venv, against the running `homemade-db-1` Postgres container) on a scratch port (8010, to avoid touching an unrelated pre-existing backend process already bound to 8000) and ran a full curl cycle with a cookie jar, `Origin: http://127.0.0.1:5173` on every mutating call: `POST /api/auth/register` → 201; `POST /api/auth/login` → 200, `Set-Cookie` attributes for both cookies matched spec exactly (see above); `GET /api/auth/me` → 200 with the registered email; `POST /api/auth/refresh` → 200, and the returned `refresh_token` cookie value differed from the pre-refresh value (rotation confirmed, not just a 200 status check); `POST /api/auth/logout` → 200; `GET /api/auth/me` again (same jar, post-logout) → 401; `POST /api/auth/login` **without** the `Origin` header → 403 `"Origin not trusted"` (CSRF dependency confirmed working, not just present in code). Test user (`task8verify@example.com`) and its 2 `refresh_tokens` rows deleted from Postgres afterward via a one-off script using `database/db.SessionLocal` + `database/models`.
  2. **Full manual browser click-through — deliberately deferred, not skipped.** The plan's Task 8 Step 2 (Playwright headless-Chromium click-through: register→onboarding→home, reload-survives-session, DevTools cookie inspection, logout, logout-during-backend-down failure path) requires installing Playwright + downloading Chromium — a new-package install the user explicitly declined to do right now. **User will do this browser click-through manually themselves.** This is a scope decision, not a gap discovered late — flagging it here so it isn't misread as "Task 8 finished, browser was verified."
  - Automated regression: full backend suite `python -m unittest discover -s tests -v` → **62/62 passing** (this is the correct, stable count — confirmed identical to what Task 4's own report recorded right after Tasks 1-4 landed; if a future task brief says "74 tests", that number was wrong, not this run)
  - **Not done yet (explicitly out of this phase's scope, staged for later):**
    - Phase 2: migrate the remaining endpoints (`/api/recipes`, `/api/user-ingredients`, etc.) from the `get_dev_user_id`/`DEV_USER_EMAIL` dev-bridge (see step 8 note further down this file) to real `get_current_user` — grep `get_dev_user_id` in `main.py` to find every call site that still needs this
    - Phase 3: real SMTP-backed OTP for password reset (`OtpVerify.jsx`/`ResetPasswordEmail.jsx` currently mock `requestOtp`/`verifyOtp` client-side only — no backend email send exists)
  - Files touched this task: `spec.md` only (verification task — no `/backend/` or `/frontend/` code edits)
- **Post-final-review fix wave (2026-09-08, same branch, after Task 8) — fixed 2 findings from the whole-branch final review, no new scope:**
  - **I-1 (proven bug, fixed):** `backend/main.py`'s `/api/auth/refresh` 401 path and `/api/auth/logout` 500 path called `clear_auth_cookies(response)` then `raise HTTPException(...)` — dead code, because FastAPI only merges an injected `Response` parameter's headers into the final response when the handler returns normally; a raised `HTTPException` makes Starlette build a fresh response and silently drops any `Set-Cookie` already set on that `Response` object. Fixed by building a `fastapi.responses.JSONResponse` directly (same status code + `{"detail": ...}` body FastAPI's default handler would have produced), calling `clear_auth_cookies` on *that* response, and `return`ing it instead of raising — no change to `verify_same_origin`/CSRF or any other route. Also added `db.rollback()` before returning in `/logout`'s except block (previously missing; harmless in practice since `get_db`'s `finally: db.close()` runs right after, but correct hygiene now that the block is being touched). Verified two ways: (1) new regression test `test_refresh_with_invalid_cookie_clears_both_cookies_on_401` in `backend/tests/test_auth_endpoints.py` asserts the 401 response's `Set-Cookie` headers actually clear `access_token` (`Path=/api`) and `refresh_token` (`Path=/api/auth`) — the exact gap the bug report described, not just a 401 status check; (2) live curl against `homemade-db-1` Postgres on scratch port 8010: `POST /api/auth/refresh` with a garbage/no `refresh_token` cookie now returns `401` **with** both `Set-Cookie: ...Max-Age=0...` headers present (previously absent).
  - **I-2 (env vars undocumented, fixed):** `COOKIE_SECURE` (`backend/auth.py`) and `TRUSTED_ORIGINS` (`backend/csrf.py`) existed only in code + the design doc, not in `backend/.env`, no `.env.example`, and this spec never named them. Documenting here (no `.env.example` created — smaller, sufficient fix): `COOKIE_SECURE` defaults `"false"` (cookies sent without the `Secure` attribute — fine for local HTTP dev, **must be set `COOKIE_SECURE=true` in any production deployment sitting behind real HTTPS**, or browsers will still accept the auth cookies over plain HTTP); `TRUSTED_ORIGINS` defaults `"http://127.0.0.1:5173,http://localhost:5173"` (comma-separated, used by `verify_same_origin`'s CSRF check) and **must be set to the real production frontend origin(s)** before deploy, or every mutating auth request will 403.
  - Also recording (previously unrecorded — Task 8 had descoped frontend checks): verified independently during the final whole-branch review (not re-run in this fix wave) — `vite build` exit 0, 2185 modules; `eslint` 0 new errors introduced by this branch vs. the pre-existing baseline.
  - Verification: `backend/tests/test_auth_endpoints.py` isolated run (`unittest discover -s tests -p "test_auth_endpoints.py" -v`) → **13/13 passing** (12 existing + 1 new regression test); full backend suite (`unittest discover -s tests -v`) → **63/63 passing**.
  - Files touched this wave: `backend/main.py` (the two error paths described above + the `JSONResponse` import), `backend/tests/test_auth_endpoints.py` (new regression test), `spec.md` (this entry).

**Reskin auth + onboarding pages with friend's pages_v2 design (frontend-only, branch `Not-FrontEnd-Design`, 2026-09-02) — product envelope, ไม่ใช่ contribution ที่ต้องมี evaluation:**
- เพื่อนร่วมทีม push การออกแบบ UI ใหม่ (Figma-driven) มาที่ branch `Not-FrontEnd-Design` เป็นไฟล์เดี่ยว 12 ไฟล์ใน `frontend/src/pages_v2/` (ยังคงอยู่ในนั้น ไม่ลบ) — เป็น visual mockup ล้วนๆ ไม่มี `useAuth()` wiring, ไม่มี shared components ที่มันอ้างถึง, ไม่มี `react-mobile-picker`, ไม่มี Tailwind tokens สำหรับ class name ที่ใช้ — ทำงานเอาไปสวมทับ auth/onboarding pages ที่มีอยู่แล้วโดยตรงไม่ได้ ต้อง port เฉพาะ visual layer เข้าไปแทน
- เพิ่ม `@theme` block ใน `frontend/src/index.css` — token สี/ขนาดตัวอักษร (`background-primary`, `button-primary`, `text-brands`, `text-h2`, `stroke-text-field`, `forms-otp-fill-*` ฯลฯ) **derived มาจากค่า hardcode เดิม** (`#EF5A3A`, gray-50/300/400/700) ไม่ใช่ค่าจริงจาก Figma — เป็น placeholder รอค่า Figma จริงจากเพื่อน
- Component ใหม่ 3 ตัวใน `frontend/src/components/`: `HeaderLogo.jsx`, `BackButton.jsx`, `NextButton.jsx` (`BackButton`/`NextButton` รับได้ทั้ง `to` route หรือ `onClick` — Onboarding ใช้ `onClick` เพราะยังเป็น single-route)
- Restyle ทุกหน้า auth (`Login`, `Register`, `ResetPasswordEmail`, `OtpVerify`, `ResetPasswordConfirm`, `Loading`) ด้วย markup/classes จาก pages_v2 — **logic เดิมคงไว้ทั้งหมด** (`useAuth()` calls, guard `useEffect`s) ไม่ได้ตัดออกแบบไฟล์ pages_v2 ดิบๆ; คงฟิลด์ email (ไม่ใช่ phone ที่ pages_v2 ใช้ เพราะ mock `requestOtp` validate email เท่านั้น), คง OTP 6 หลัก (ไม่ใช่ 4 ตามดีไซน์ เพราะ mock `verifyOtp` เช็ค length 6), คง `PasswordInput` (feature show/hide toggle จาก commit `ec4f6641`) แทนที่จะใช้ bare input ตามดีไซน์
- **Onboarding ยังเป็น single route** (`/onboarding`, internal `step` state 1–6) ตามเดิม ไม่ได้แยกเป็น 6 route ตามที่ pages_v2 แยกไฟล์ไว้ (ตัดสินใจแล้วกับผู้ใช้ว่าไม่คุ้มกับ state-plumbing ที่ต้องเพิ่ม) — restyle เฉพาะ visual แต่ละ step ด้วย classes จาก `QuizStep1-6.jsx`
- **Age picker เปลี่ยนจาก birthdate wheel (เดิม, custom CSS scroll-snap) เป็น age number 15-80 ด้วย `react-mobile-picker`** (ติดตั้งแล้ว ตัดสินใจร่วมกับผู้ใช้แล้ว) — `submitProfile()` payload เปลี่ยน field `birth_date` → `age` (number); payload shape ปัจจุบัน: `age, cuisine_preferences[], dietary_restrictions[], equipment[], cooking_frequency, cooking_goals[]`
- คง "other" free-text input ใน step 3/4 (อาการแพ้/อุปกรณ์ "อื่น ๆ") ไว้ตามเดิม แม้ pages_v2's static mock ไม่มี field นี้โชว์ — เป็น UX ที่มีอยู่แล้วไม่อยากตัดทิ้ง
- **พบ + แก้ pre-existing race condition ระหว่างทาง** (ไม่เกี่ยวกับงาน reskin โดยตรง): `ResetPasswordConfirm.jsx`'s guard `useEffect` เดิม watch `otpVerified` reactively — แต่ `resetPassword()` เองก็ clear `otpVerified` เป็นส่วนหนึ่งของ success cleanup ทำให้ guard redirect ไป `/reset-password` แข่งกับ `navigate("/login")` ที่ตั้งใจไว้ ผลคือ flow reset password ไม่เคย complete จริงแม้แต่ในโค้ดเดิมก่อนงานนี้ (เพิ่งเจอเพราะทดสอบ click-through เต็ม flow ครั้งแรก) — แก้ด้วยการเช็ค `otpVerified` ครั้งเดียวตอน mount (`useRef`) แทนที่จะ reactive
- Verify: `npm run build` exit 0, `npm run lint` ไม่มี error ใหม่จากไฟล์ที่แก้ (baseline เดิม 4 error ไฟล์อื่นที่ไม่เกี่ยวยังอยู่เหมือนเดิม, `pages_v2/*.jsx` มี unused `logo` import warning อยู่ก่อนแล้วซึ่งเป็นไฟล์ต้นฉบับของเพื่อน ไม่ได้แก้), **ทำ full browser click-through verification จริงด้วย Playwright** (dev server `--host 127.0.0.1 --port 5173 --strictPort` + headless Chromium): Login → Register → Onboarding ครบ 6 step (age picker, chip toggle, allergy "none" exclusivity, "other" text input, back-button ไม่หลุด route, submit) → กลับ Login เห็น success message → Forgot password → กรอก email → OTP 6 หลัก → set password ใหม่ → กลับ Login เห็น success message อีกครั้ง — console errors: 0 ตลอดทั้ง flow
- ไฟล์ที่แก้: `frontend/src/index.css`, `frontend/src/components/PasswordInput.jsx`, `frontend/src/pages/auth/{Login,Register,ResetPasswordEmail,OtpVerify,ResetPasswordConfirm,Loading,Onboarding}.jsx`, `frontend/package.json`+lock (เพิ่ม `react-mobile-picker`); ไฟล์ใหม่ `frontend/src/components/{HeaderLogo,BackButton,NextButton}.jsx` — ไม่แตะ `/backend/`, ไม่แตะ/ลบไฟล์ใน `pages_v2/` (ของเพื่อน เก็บไว้เป็น reference)

**Home search + filter panel (frontend-only, 2026-08-29) — เข้าเงื่อนไข "product envelope" ไม่ใช่ contribution ที่ต้องมี evaluation:**
- `App.jsx` search box (เดิมเป็น placeholder เฉยๆ) ตอนนี้ filter `filteredRecipes` จริงตาม `recipe.name` (case-insensitive substring)
- ปุ่ม filter (`SlidersHorizontal`) เปิด overlay panel — ตำแหน่ง top วัดจาก `searchBarRef.offsetTop + offsetHeight` ด้วย `useLayoutEffect` (anchor ใต้ช่อง search พอดี ไม่ hardcode pixel), scroll ภายใน panel ซ่อน scrollbar ด้วย `.scrollbar-hide` util เดิม (ใช้ pattern เดียวกับ `RecipeDetail.jsx`/`CookingPage.jsx`)
- 5 tab: Ingredient, Meal Type, Dietary, Cuisine, Occasion — badge โชว์ `(n)` เมื่อ tab นั้นมีการเลือก
  - Ingredient tab: search ในตัว, section Selected (มีปุ่ม X มุมขวาบนของรูปไว้เอาออก), section All Ingredient (ดึง unique ingredient จาก `recipes[].ingredients` จริง ไม่ mock, dim เมื่อเลือกแล้ว)
  - อีก 4 tab: checkbox list มี emoji นำหน้าแต่ละ option (`OPTION_EMOJI` map)
- **⚠️ ทราบแล้วและตัดสินใจร่วมกับผู้ใช้แล้ว (ไม่ใช่บั๊ก):** ตัวเลือกใน Meal Type/Dietary/Cuisine/Occasion (Breakfast, Vegan, Thai, Date Night ฯลฯ) เป็น label ที่ผู้ใช้ระบุเอง **ไม่ตรงกับ `tags` จริงใน `backend/database/setup_db.py`** (มีแค่ "Asian Food", "Thai Food", "Healthy / Diet", "Western Food", "High Protein", "Spicy", "Quick & Easy") — logic filter เทียบกับ `recipe.tags` ตรงๆ (case-insensitive) ตามที่ผู้ใช้เลือก ("Build UI, filter against tags as-is") ดังนั้น 4 tab นี้จะไม่ match recipe ไหนเลยจนกว่า backend จะเพิ่ม tags ที่ตรงกับ options เหล่านี้จริง — ไม่ใช่ NO MAGIC violation เพราะไม่ได้เดา/หลอกว่า match ได้ ผู้ใช้รับทราบ gap นี้แล้ว
- Filter tab badge row เพิ่ม mouse click-drag-to-scroll (สำหรับใช้บนคอม/ไม่มี touch) — มี threshold 5px กัน drag ที่ตั้งใจ scroll ไป trigger tab-switch click โดยไม่ได้ตั้งใจ
- Clear ล้างทุก selection ทั้ง 5 category (panel ยังเปิดอยู่), Apply แค่ปิด panel (selection apply แบบ live ระหว่างติ๊กอยู่แล้ว ไม่มี draft/commit state แยก — ตัดสินใจให้ minimal ตามขอบเขตที่ขอ)
- Verify: `npx eslint src/App.jsx` ผ่าน 0 error/warning, `npx vite build` exit 0 (ทำซ้ำ 3 รอบระหว่างแก้ทีละส่วน ครั้งล่าสุดผ่านหมด), ผู้ใช้ทดสอบผ่าน dev server เองแล้วว่าใช้งานได้ (confirm ในแชท "ลอง dev server แล้วเรียบร้อย")
- ไฟล์ที่แก้: `frontend/src/App.jsx` เท่านั้น (เพิ่มฟังก์ชัน `FilterCheckboxList` ในไฟล์เดียวกัน) — ไม่แตะ `/backend/`, ไม่ติดตั้ง package ใหม่

**User Profiling onboarding wizard (frontend-only, 2026-08-24) — เข้าเงื่อนไข "product envelope" ไม่ใช่ contribution ที่ต้องมี evaluation:**
- ไฟล์ใหม่ `frontend/src/pages/auth/Onboarding.jsx` — 6-page wizard เก็บ birthdate, cuisine preferences, dietary restrictions, equipment, cooking frequency, cooking goals; internal `step` state (1–6) ในไฟล์เดียว มี back/next ทุกหน้า
- Route ใหม่ `/onboarding` เพิ่มใน `frontend/src/main.jsx`; `Register.jsx` เปลี่ยนจาก navigate ตรงไป `/login` เป็น navigate ไป `/onboarding` หลัง register สำเร็จ (mock), จบ wizard แล้ว navigate ต่อไป `/login`
- ~~Birthdate เป็น custom wheel picker~~ **แทนที่แล้วด้วย age picker (`react-mobile-picker`) — ดูหัวข้อด้านบน (2026-09-02)**
- เก็บผลเป็น local state เท่านั้น ยังไม่มี backend call — มี stub function `submitProfile()` ใน `Onboarding.jsx` พร้อม comment `TODO(backend): POST /api/user-preferences once SQLite -> PostgreSQL migration lands` รอ backend พร้อมค่อยผูกจริง (payload shape อัปเดตแล้วด้านบน)
- ไฟล์ที่แก้: `frontend/src/main.jsx`, `frontend/src/pages/auth/Register.jsx`, ไฟล์ใหม่ `frontend/src/pages/auth/Onboarding.jsx` — ไม่แตะ `/backend/`

**AddIngredient image picker → online search + upload (2026-08-23) — product envelope, ไม่ใช่ contribution ที่ต้องมี evaluation:**
- เดิม `frontend/src/pages/AddIngredient.jsx` เลือกรูปจาก local library ที่ backend list ให้ (`GET /api/ingredient-images`); ตอนนี้เปลี่ยนเป็น **online image search** (Pexels) พิมพ์ค้นหา/auto-search ตามชื่อวัตถุดิบ debounce 400ms, ถ้า search fail หรือหา provider ไม่เจอมีปุ่ม **Upload or Take Photo** (`<input type="file" accept="image/*" capture="environment">`) เป็น fallback
- Backend เพิ่ม 2 endpoints ใหม่ใน `backend/main.py` (ขอ confirm แล้วก่อนแก้ตาม Hard Rule):
  - `GET /api/ingredient-images/search?q=<term>` — proxy เรียก Pexels Search API ด้วย `PEXELS_API_KEY` (server-side, ไม่ expose key ให้ frontend), คืน list URL รูป (`photo.src.medium`)
  - `POST /api/ingredient-images/upload` (multipart) — save ไฟล์ลง `backend/images/ingredients/` ด้วยชื่อสุ่ม (`uuid4().hex` + นามสกุลเดิม), คืน URL แบบเดียวกับรูป static เดิม
  - `requests` package เพิ่มใน `backend/requirements.txt` แต่ **ไม่ต้อง pip install ใหม่** เพราะติดมากับ `google-genai` dependency อยู่แล้ว (เช็คแล้วด้วย `python -c "import requests"` ผ่าน)
- `GET /api/ingredient-images` (list local library) ยังอยู่เหมือนเดิม — ตอนนี้ frontend ใช้แค่ดึง `fallback` URL (default "no image" placeholder) เท่านั้น ไม่ได้ใช้ full list แล้ว
- **ค้างอยู่:** `PEXELS_API_KEY` ยังไม่ได้ตั้งใน `backend/.env` — ผู้ใช้จะสมัคร/ใส่เองทีหลัง จนกว่าจะใส่ search endpoint จะคืน `{"status":"error","message":"PEXELS_API_KEY not configured"}` เสมอ (ทดสอบแล้วว่า error graceful ไม่ crash, frontend แสดงข้อความ error ชี้ไปปุ่ม upload แทน)
- Verify: `python -m py_compile backend/main.py` ผ่าน, `bun run build` (frontend) exit 0, ทดสอบ live: dev server (`--host 127.0.0.1 --port 5173 --strictPort`) HTTP 200, backend `uvicorn` local เรียก `/api/ingredient-images/search` เจอ error ตามคาด, เรียก `/api/ingredient-images/upload` ด้วยไฟล์ทดสอบ (`No-image-available.png`) สำเร็จ ได้ URL คืนมา และ URL นั้น serve ได้จริง (HTTP 200) — ลบไฟล์ทดสอบที่ upload ออกหลังตรวจแล้ว ไม่ทดสอบผ่าน browser UI จริง (ผู้ใช้ควรตรวจหน้า AddIngredient เองผ่าน dev server อีกที โดยเฉพาะหลังใส่ `PEXELS_API_KEY` แล้ว)
- ไฟล์ที่แก้: `backend/main.py`, `backend/requirements.txt`, `frontend/src/pages/AddIngredient.jsx`

**Profile page (frontend-only, 2026-08-18) — เข้าเงื่อนไข "product envelope" ไม่ใช่ contribution ที่ต้องมี evaluation:**
- ไฟล์ใหม่ `frontend/src/pages/Profile.jsx` ผูกกับ bottom-tab "me" ผ่าน `App.jsx` (`currentView === "profile"`); internal sub-view switching ในไฟล์เดียว (main/history/language/preferences/helps) ตาม pattern เดียวกับ `CreateRecipe.jsx`
- Username/email/avatar เป็น **hardcoded placeholder** — ยังไม่มี auth/user backend (เช็คแล้วว่า `/backend/` ไม่มี endpoint user/profile ใดๆ) ตรงตาม NO MAGIC ห้ามเดา infra
- Cooking History ดึงจาก `cookingHistory` state ใหม่ใน `App.jsx` — push เข้า array ทุกครั้งที่ `generate-recipe-text` สำเร็จ (ทั้งสอง flow: create-recipe และ custom-cooking) เก็บ **in-session เท่านั้น** รีเฟรชหน้าแล้วหาย เพราะยังไม่มี persistence/backend
- Language menu: เลือกได้ (ไทย/English) แต่เป็น **visual only ไม่มี i18n wiring จริง** (ยังไม่มี locale infra ในโปรเจกต์)
- Preferences menu: ใช้ pill-select pattern เดิมจาก `CreateRecipe.jsx` (รสชาติ/อาการแพ้อาหาร/อุปกรณ์ที่มี) เก็บเป็น local state ใน `Profile.jsx` เท่านั้น **ยังไม่ได้ผูกเป็นค่า default ให้ `CreateRecipe.jsx`** จริง (ไม่ได้อยู่ในขอบเขตที่ขอ ถ้าจะทำเป็นงานถัดไป)
- Logout button: แดง วางไว้ตาม UI spec แต่ยังเป็น **no-op** (`console.log` เฉยๆ) เพราะยังไม่มี auth/session backend ให้ logout จริง
- Verify: `npx eslint` ผ่าน (0 error/warning บนไฟล์ที่แก้), `npx vite build` exit 0 — **ไม่ได้ทำ visual browser verification** (playwright chromium cache version ไม่ตรงกับที่ npx ต้องการ ต้องโหลดใหม่ ผู้ใช้เลือก skip ไม่ดาวน์โหลด) ผู้ใช้ต้องตรวจ UI จริงเองผ่าน dev server
- ไฟล์ที่แก้: `frontend/src/App.jsx` (เพิ่ม `cookingHistory` state, route `"profile"`, tab handler `"me"`), ไฟล์ใหม่ `frontend/src/pages/Profile.jsx` — ไม่แตะ `/backend/`, ไม่ติดตั้ง package ใหม่

**Local development launcher (implemented and locally verified — 2026-07-14):**
- เป้าหมาย: ดับเบิลคลิก Windows Desktop shortcut ครั้งเดียวเพื่อเปิด FastAPI backend, Vite frontend และ `http://127.0.0.1:5173` ใน default browser โดยไม่ต้องเปิด VS Code/activate venv เอง
- launcher จะอยู่ที่ root ชื่อ `start-homemade.ps1` และต้องถูก `.gitignore` แบบเจาะจงเพื่อไม่ให้ push ขึ้น GitHub; Desktop shortcut อยู่นอก repo
- ใช้ `backend/venv/Scripts/python.exe -m uvicorn main:app --reload` และ `bun run dev -- --host 127.0.0.1 --port 5173 --strictPort`; ไม่ติดตั้ง package และไม่แก้ `/backend/`
- ต้องรอ HTTP response จากทั้ง FastAPI (`/docs`) และ frontend ก่อนเปิด browser, แสดง log แยกสอง terminal และตรวจ syntax/HTTP/shortcut/`git check-ignore` ก่อนถือว่าเสร็จ
- Design: [`docs/superpowers/specs/2026-07-14-local-dev-launcher-design.md`](docs/superpowers/specs/2026-07-14-local-dev-launcher-design.md)
- PowerShell AST parse: 0 errors
- git check-ignore: `/start-homemade.ps1` matched
- FastAPI `/docs`: HTTP 200
- Vite `/`: HTTP 200
- Desktop shortcut target/arguments/working directory: verified
- Final shortcut smoke test: browser opened frontend; dev terminals left running for user

**เสร็จแล้ว:** ข้อ 1 — `backend/main.py` แก้เป็น critic-feedback loop จริงแล้ว:
- `call_agentic_llm()` รับ `feedback=None` param เพิ่ม, ถ้ามีค่าจะฝังเป็น `feedback_section` ลงใน prompt บอก Gemini ว่ารอบก่อนพังเพราะอะไร ต้องแก้อะไรโดยเฉพาะ
- retry loop ใน `generate_recipe_text()` เก็บ `result["reason"]` จาก validation ที่ fail ไปเป็น `feedback` ป้อนรอบถัดไป (แทนที่จะ retry มั่วด้วย input เดิมทุกรอบ)
- verify แล้วด้วย `python -m py_compile main.py` → syntax OK (ยังไม่ได้ integration test จริงกับ Gemini API — ต้องรันเซิร์ฟเวอร์ทดสอบก่อนถือว่า verify ครบ)
- commit `0949f304` + push ขึ้น `origin/main` แล้ว (2026-07) — commit เฉพาะ `backend/main.py` เท่านั้น
- README.md อัปเดตเพิ่มบูลเล็ต "Adaptive Retry" ให้ตรงกับพฤติกรรมใหม่ → commit `a7371706` + push แล้วเช่นกัน
- `.gitignore` และ `backend/database/recipes.db` ยังค้าง modified อยู่ (ไม่เกี่ยวกับ fix นี้ ยังไม่ commit — ถามผู้ใช้ค้างไว้ว่าจะจัดการยังไง)

**Quantity/presence grounding slice (2026-07-14) — implement แล้วและตรวจสอบในเครื่องแล้ว:**
- My Fridge เป็น **presence-only**: active UI/API create/list ไม่รับ ไม่แสดง และไม่คืน `quantity`; payload สำหรับ generate ส่งวัตถุดิบเป็น `{id?, name}` และ backend ใช้เฉพาะชื่อ แม้ยังยอมรับ legacy string/object metadata เพื่อ compatibility (`quantity` ที่ติดมากับ legacy object จะถูก ignore)
- `servings` คือจำนวนที่เสิร์ฟ; ปริมาณใน `adjusted_ingredients` เป็นปริมาณรวมสำหรับสูตรทั้งสูตรที่ครอบคลุมจำนวนที่เสิร์ฟนั้น ส่วน `nutrition` (`calories`, `protein_g`, `carbs_g`, `fat_g`) เป็นค่าต่อ 1 ที่เสิร์ฟตาม `basis: "per_serving"` ไม่ใช่ stock คงเหลือใน My Fridge
- ไม่มี shortage/“ต้องซื้อเพิ่ม” claim หรือข้อสรุปว่า stock “เพียงพอ”, ไม่มีการเปรียบเทียบปริมาณคงเหลือ และไม่มี stock deduction หลังทำอาหาร เพราะระบบไม่ได้เก็บปริมาณที่ใช้จริง; validation stage 3 ปฏิเสธข้อสรุปเรื่อง stock ทั้งด้านไม่พอ/ต้องซื้อและด้านเพียงพอที่หลุดมาใน `instructions` หรือ `safety_warning` แล้วส่งเหตุผลกลับ critic-feedback retry
- ก่อนเข้า 6 validation stages มี contract preflight ตรวจ top-level object, `recipe_name`, `servings`, non-empty `adjusted_ingredients`/`instructions`, `diet_tags` (ต้องเป็น list ของ nonblank strings แต่ empty list ได้), `safety_warning` (nonblank string), `nutrition.basis` และ nutrient ทั้ง 4 ค่า; nutrient ต้องเป็น finite number ที่ไม่ใช่ boolean และไม่ติดลบ โดยกฎ calories เดิม `> 0` และ `<= 1500` ยังอยู่ใน stage 4 (preflight นี้ไม่ใช่ stage ที่ 7)
- Cooking Page แสดงคำเตือนทั่วไปตามข้อความ exact: **“ตรวจสอบวัตถุดิบและปริมาณจริงก่อนเริ่มทำอาหาร”**
- Item-specific notice เช่น “ไม่พบในรายการ My Fridge — กรุณาตรวจสอบว่ามี” ยัง defer ไป slice หลัง canonical ingredient resolver พร้อมแล้วเท่านั้น; ห้ามเทียบด้วย raw Thai/English string
- SQLite column `user_ingredients.quantity` เป็น legacy/deprecated และคงไว้เพื่อ compatibility เท่านั้น: implementation ไม่ rewrite ค่าเดิมและ insert แถวใหม่ด้วย `quantity = NULL`
- หลักฐาน local ล่าสุดหลัง final-review second pass: frontend contract tests 9/9 PASS, Vite build exit 0, lint คง baselineเดิม 3 errors/0 warnings (ผล frontend จาก first pass เพราะ second pass ไม่แก้ frontend); backend pure + mocked FastAPI handler tests 25/25 PASS และ `main.py`/`recipe_contracts.py` compile ผ่าน การทดสอบ FastAPI handler patch `call_agentic_llm` จึงไม่เรียก live Gemini, network หรือ server
- การทดสอบใช้ in-memory DB และ final verification ไม่เปลี่ยน `backend/database/recipes.db` (SHA256 ก่อน/หลัง final verification เท่ากัน: `7E647EE9AAA524941B20D792CDCBBE7786A1BF457DA66EDD23C01B0AEA941136`; แถว read-only ยังคงเป็น ids 19, 21, 22, 23, 24 พร้อมค่า quantity เดิม) อย่างไรก็ดี DB modified อยู่ก่อน final verification และไม่มี row/hash snapshot ก่อน Tasks 1–6 เริ่ม จึง **ไม่อ้าง** ว่า hash นี้พิสูจน์ความไม่เปลี่ยนแปลงตลอด implementation ทั้งหมด
- Design (local/ignored, ไม่ stage/commit): [`docs/superpowers/specs/2026-07-14-presence-only-fridge-design.md`](docs/superpowers/specs/2026-07-14-presence-only-fridge-design.md)
- Implementation plan (local/ignored, ไม่ stage/commit): [`docs/superpowers/plans/2026-07-14-presence-only-fridge.md`](docs/superpowers/plans/2026-07-14-presence-only-fridge.md) — item-specific badge และ quantity parseability validation ยังคง defer ตามขอบเขตด้านบน

**System Architecture diagram (Eraser.io) — งานนี้คือแก้ "ภาพ" ใน Eraser เท่านั้น ไม่ใช่แก้โค้ด (บทเรียนจากจุดที่ 1: ต้องเจาะจงเองว่าหมายถึง diagram หรือโค้ด — อย่าเดา):**

1. ~~"Retry (Max 3 times)" → Feedback loop~~ ✅ diagram แก้แล้ว **และ** โค้ดแก้แล้ว (`main.py`) — จุดนี้เสร็จสมบูรณ์ทั้งสองฝั่ง
2. ~~Allergen 2-layer แยกเป็น box "Allergen Validation (2-layer)" นอก 6-Step Check list~~ ✅ **diagram แก้แล้ว** (2026-07) — **⚠️ โค้ดยังไม่แก้** `check_allergy()` ใน `main.py` ยังเป็น rule-based ชั้นเดียวเหมือนเดิม (ยังไม่มี Knowledge Graph หรือ LLM hidden-allergen layer จริง) ถ้าจะ implement ต้องขอ confirm แยกเป็นงานใหม่ (เข้าเงื่อนไข Hard Rule ห้ามแก้ backend โดยไม่ขอก่อน)
3. ~~เส้น "Preferences" เข้า Agentic Prompt Engine~~ ✅ **diagram แก้แล้ว** (2026-07) — เปลี่ยน label เป็น `"Base Recipe + Ingredients + Preferences (+ History)"` แยก History ออกจาก Preferences ชัดเจน — **⚠️ ยังไม่ได้ implement personalization module จริง** (โค้ดยังไม่มีการดึง history มาฝังใน prompt เลย เป็นแค่ diagram ที่วาดล่วงหน้าไว้)
4. ~~เพิ่ม "Users DB"~~ ✅ **diagram แก้แล้ว** (2026-07) — เพิ่ม box "User DB" ใน Data Layer พร้อมลูกศร `FastAPI --"Query User Information"--> User DB` — **⚠️ ยังไม่ได้เริ่ม auth module จริง** (ยังไม่มีตาราง users, ยังไม่มี JWT/login endpoint ในโค้ด)

**สรุป:** รีวิว System Architecture diagram ครบทั้ง 4 จุดแล้ว (ฝั่ง diagram) — สิ่งที่ diagram แสดงตอนนี้คือ**เป้าหมายที่จะสร้าง** ไม่ใช่สถานะโค้ดปัจจุบัน ยังมีช่องว่างระหว่าง diagram กับโค้ดจริงอยู่ 3 โมดูลใหญ่ที่ยังไม่เริ่ม: Allergen Knowledge Graph + LLM layer, Personalization module, Auth/Users module (ทั้งหมดอยู่ใน `/backend/` — ต้องขอ confirm แยกเป็นรายโมดูลก่อนเริ่มแก้โค้ดตาม Hard Rule)

**ค้างอยู่ (ยังไม่ทำ):** บีบ Gantt chart ใน `docs/project-proposal.md` จาก ~12 เดือนเหลือ ~9 เดือนจริง (รู้ว่า scope พอ/เต็มสำหรับ 9 เดือนแล้ว ไม่ต้องเพิ่มอะไรอีก — ทีมเคยสร้าง prototype เดิมเสร็จใน 1-2 สัปดาห์ แต่ความเร็วนั้น**ไม่ transfer**ไปงาน evaluation/research ที่เหลือ เพราะ bottleneck ต่างกัน)

## Data Contracts

**`POST /api/generate-recipe-text`** (active contract):
```
Request:  { recipe: {...base_recipe}, ingredients: [{id?, name}], preferences: { allergy, taste, equipment, extra } }
Response: { status: "success"|"error", data: {recipe_name, servings, adjusted_ingredients[], diet_tags[], nutrition{basis:"per_serving",calories,protein_g,carbs_g,fat_g}, instructions[], safety_warning} }
```
Backend ยังรับ legacy ingredient strings/objects ที่มี metadata เพื่อ compatibility แต่ normalize เป็นชื่อเท่านั้นและ ignore `quantity`; FastAPI handler boundary ตรวจด้วย integration test ที่ mock `call_agentic_llm` แล้ว ไม่ใช่การทดสอบกับ live Gemini/network/server

**`GET /api/user-ingredients`** (active presence-only contract):
```
Request:  none
Response: { status: "success"|"error", data: [{id, name, category, image, selected}] }
```
เมื่อสำเร็จ `data` ไม่คืน legacy `quantity`; เมื่อ error จะคืน `message` แทน `data`

**`POST /api/user-ingredients`** (active presence-only contract):
```
Request:  { name, category, image }
Response: { status, data: {id, name, category, image, selected} }
```
⚠️ ยังไม่มี `user_id` — ต้องเพิ่มตอน migrate ไป PostgreSQL พร้อม auth

**`GET /api/ingredient-images/search`** (active contract, 2026-08-23):
```
Request:  query param q: string
Response: { status: "success"|"error", data?: {images: string[]}, message?: string }
```
`status: "error"` เมื่อ `PEXELS_API_KEY` ไม่ได้ตั้งใน `.env` หรือเรียก Pexels API ไม่สำเร็จ — frontend ต้อง fallback ไปปุ่ม upload

**`POST /api/ingredient-images/upload`** (active contract, 2026-08-23):
```
Request:  multipart/form-data, field "file" (.png/.jpg/.jpeg/.webp เท่านั้น)
Response: { status: "success"|"error", data?: {image: string}, message?: string }
```
ไฟล์ถูก save ลง `backend/images/ingredients/` ด้วยชื่อสุ่ม (`uuid4`), ยังไม่มี size limit หรือ virus/content scan — เหมาะกับ local dev เท่านั้น ต้องทำเพิ่มก่อน production

**ยังไม่ได้ออกแบบ (ต้องทำก่อนเริ่ม auth):** `/api/auth/register`, `/api/auth/login` (OAuth2PasswordRequestForm → JWT), `/api/barcode-lookup`, `/api/favorites`, `/api/ratings`, `/api/history`, `/api/user-preferences` (payload shape ร่างไว้แล้วใน `Onboarding.jsx`'s `submitProfile()` stub — ยังไม่มี endpoint จริง)
## Research note (2026-07-15)

- Official CPI reference checked: TPSO/Ministry of Commerce annual CPI release for December 2568 and year 2568.
- Food and non-alcoholic beverages CPI: 2568 compared with 2567 increased 1.05% on an annual-average basis (AoA; January–December average), not a December-only YoY figure.
- Source: https://uploads.tpso.go.th/economic/pdf/Cpig122568_tg.pdf

## Presentation note (2026-07-15)

- The current Eraser system architecture is too detailed for a presentation slide because it mixes the primary user flow, implementation internals, data stores, external services, and future research components.
- Recommended slide abstraction: show one left-to-right flow: User/Fridge Input -> FastAPI Orchestrator -> AI Recipe Generation -> Validation + Critic Retry -> Validated Recipe/UI.
- Keep Open Food Facts, Gemini, and the database as small supporting components around the orchestrator. Collapse individual databases into one Data Store group and show the three research contributions as short badges: Nutrition Engine, Allergen Graph, and Critic-Feedback Loop.
- Do not show every validation sub-check or every database arrow on the main slide. Treat the diagram as a target/research architecture and label unimplemented modules as future work where needed.
- Slide mockup created in the thread visualization workspace: `homemade-architecture-slide.html`; use it as a visual reference for the PowerPoint redraw.
- Speaking script drafted for presentation pages 7–14 (4–5 minutes), centered on the system architecture. The script distinguishes the current prototype (React/Vite/Tailwind, FastAPI + SQLite, Gemini, existing validation and critic-feedback loop) from target/future modules (PostgreSQL, nutrition engine, allergen graph/LLM layer, auth and personalization). Exact page titles were not available in the workspace, so the page mapping is an assumed narrative order based on the architecture proposal.
- The source presentation PDF was reviewed directly on 2026-07-15. The page mapping is now verified against pages 7–14, and the finalized Thai speaking script is saved at `docs/presentation-script-pages-07-14.md` with about two minutes allocated to the system architecture slide.
